# Examples

The test fixtures demonstrate the supported anonymous input shapes:

- `tests/fixtures/mineru_table_sample.json` is a small page-grouped MinerU-style table sample.
- `tests/fixtures/original_rows_sample.json` is a normalized record list suitable for the right side of `doc-reconcile compare`.

Do not place real reports, workbooks, full HTML dumps, or client output in this directory.
