# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

## 当前项目在做什么

```text
把已有抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

## 最新进展

```text
R7AL：安井食品 DateFac 产物 vs MinerU 产物真实对比已跑通
```

关键结果：

```text
DateFac 候选 rows = 451
MinerU blocks = 173
VERIFIED = 395
需复核 = 56
DISAGREED = 10
AMBIGUOUS = 10
MISSING_EVIDENCE = 2
11 个重点探针样例全部在 DateFac rows 中找到，并全部能被 MinerU v2 blocks 验证
推荐主输入 = content_list_v2
content_list = fallback / cross-check
readiness_gates = CLOSED
```

这说明：

```text
MinerU 产物已经能作为 DateFac 的原文证据来源。
```

但不能吹成：

```text
production_ready = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean_data 自动放行
可以批量交付客户
```

这次只是 controlled comparison dry-run，输出文件保留在本地 output 目录，没有提交。

## 当前下一步

```text
348N-R7AM test-only MinerU artifact adapter prototype
```

R7AM 要做：

```text
把 R7AL 证明可行的 MinerU content_list_v2 -> evidence block 逻辑，整理成 tests/agent/ 下的 test-only adapter prototype。
```

R7AM 不做：

```text
不修改生产代码
不提交完整 MinerU output
不提交 DateFac output
不跑 MinerU
不跑 OCR / LLM / VLM
不新增依赖
不打开 readiness gates
```

一句话：

```text
真实对比已经跑通；下一步不是急着接生产，而是把这次本地脚本里的有效逻辑收敛成可重复、可测试、可审查的小型 adapter 原型。
```
