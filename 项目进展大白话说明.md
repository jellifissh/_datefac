# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
R7AE: file-backed source_text sidecar loader 设计完成
R7AF: test-only file-backed source_text sidecar loader 实现完成
R7AF-QA: file-backed source_text sidecar loader 审查通过
R7AG: lightweight PDF evidence bridge 设计通过
```

R7AG 的重点：

```text
不要 MinerU-first
用轻量 PDF 文本层 / page text / 数字锚点 / 关键词和期间邻近关系建立证据联系
有 page_number 时只抽目标页
没 page_number 时做 capped candidate search
raw Excel/JSON excerpt 只当 hint，不当 trusted evidence
MinerU/OCR/manual review 只做 fallback
```

大白话：以后不是别人一给几十份 PDF 就默认全部跑 MinerU，而是先用便宜的文本层和锚点匹配建立证据链；跑不通的再进重型 fallback 或人工复核。

---

## 3. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7AG 的正确含义是：低成本证据桥路线设计通过，不是已经有真实 PDF parser。

---

## 4. 当前下一步：R7AH

当前下一步：

```text
348N-R7AH lightweight PDF evidence bridge test-only prototype
```

任务大小与执行方式：

```text
task_size = large
recommended_reasoning_level = max
execution_mode = phased-hard-stop + implementation-with-self-QA
```

R7AH 要做：

```text
在 tests/agent/ 下实现 test-only lightweight bridge helper
使用 synthetic page-text provider，不跑真实 PDF
用 row hints: source_document_id / page_number / metric_name / period / value
实现 value + metric + period proximity 匹配
生成 SourceTextEvidence
验证能驱动 VERIFIED
验证 value-only / wrong-page / missing-text / scanned-marker 等保守失败
验证 evidence_index / review_queue 不写 full source_text
验证无 MinerU/OCR/LLM/VLM、无 production hook
```

R7AH 不做：

```text
真实 PDF parser
新增 PyMuPDF/pdfplumber/pypdf 依赖
真实 PDF 运行
MinerU / OCR / LLM / VLM
runner / CLI / run_pilot hook
production readiness
```

---

## 5. 一句话总结

```text
项目已经把路线从重型 MinerU 默认入口，调整为轻量 evidence bridge；R7AH 要用测试原型证明“产物 + PDF页文本锚点”可以低成本生成 SourceTextEvidence。
```
