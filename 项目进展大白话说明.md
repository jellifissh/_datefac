# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
R7AE: file-backed source_text sidecar loader 设计完成
R7AF: test-only file-backed source_text sidecar loader 实现完成
R7AF-QA: file-backed source_text sidecar loader 审查通过
```

R7AF-QA 确认：

```text
loader 只在 tests/agent/
没有 datefac_agent / runner / CLI / run_pilot / production hook
JSON object v1 strict schema
坏 sidecar 整包 fail closed，不返回 partial records
evidence_index metadata-only
review_queue compact fields only
full source_text 不进入 serialized outputs
VERIFIED 不影响 STRONG_EVIDENCE / clean_data / readiness
pytest tests/agent -q = 162 passed
```

大白话：文件型 sidecar 的测试入口已经安全通过 QA，但它仍然不是生产证据入口。

---

## 3. 当前路线调整

之前自然会想到下一步直接做 MinerU adapter，但这会把成本拉高：如果别人丢几十份 PDF，就可能变成每份都要重型解析。

所以当前路线调整为：

```text
不要 MinerU-first。
先设计 lightweight PDF evidence bridge。
```

也就是先用更轻的方式建立联系：

```text
现有产物里的 page_number / metric_name / period / value
+
原 PDF 的文本层 / 页文本 / 数字锚点 / 关键词邻近
=
SourceTextEvidence sidecar
```

MinerU 只作为 fallback：

```text
文本层没有、扫描 PDF、复杂表格、轻量桥找不到证据、重要疑难项
```

---

## 4. 当前下一步：R7AG

当前下一步：

```text
348N-R7AG lightweight PDF evidence bridge design
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = design-review-only
```

R7AG 要设计：

```text
如何不用 MinerU 默认参与，也能把产物和原 PDF 建立证据联系
用哪种轻量 provider 边界，PyMuPDF/pdfplumber/pypdf 先抽象还是先依赖
有 page_number 时只抽对应页
没 page_number 时如何全文或候选页搜索
如何用 value / metric_name / period 做 anchor
如何处理重复数字、中文单位、百分比、负数
如何生成 source_text snippet、locator、hash、char_count
如何区分 provider 和 source_text_quality
什么时候进入 review_queue
什么时候 fallback 到 MinerU/OCR/manual review
如何缓存 source_file_sha256 / page_text / sidecar / manifest
```

R7AG 不做：

```text
实现 PDF parser
新增依赖
跑真实 PDF
跑 MinerU / OCR / LLM / VLM
批量 PDF 平台
production readiness
```

---

## 5. 一句话总结

```text
项目已经把 test-only source_text sidecar 入口做安全了；下一步不急着接 MinerU，而是先设计轻量 evidence bridge，把“现有产物”和“原 PDF 文本层”用低成本方式挂起来。
```
