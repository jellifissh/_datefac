# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
R7AE: file-backed source_text sidecar loader 设计完成
R7AF: test-only file-backed source_text sidecar loader 实现完成
R7AF-QA: file-backed source_text sidecar loader 审查通过
R7AG: lightweight PDF evidence bridge 设计通过
R7AH: lightweight PDF evidence bridge test-only 原型完成
R7AH-QA: lightweight PDF evidence bridge 原型审查通过
R7AI: real PDF text-layer provider 设计通过
```

R7AI 设计结果：

```text
commit = acd68dc docs: add R7AI real PDF text layer provider design
Decision = PASS，348N_R7AI_REAL_PDF_TEXT_LAYER_PROVIDER_DESIGN_VALID
pytest tests/agent -q = 180 passed
readiness_gates = CLOSED
```

R7AI 的重点：

```text
真实 PDF text-layer provider 仍然是轻量证据，不是 production readiness
MinerU 仍然是 fallback，不是 default
OCR 仍然是 fallback，不是 default
cache output 默认不提交
full source_text 不进入 evidence_index / review_queue
VERIFIED 不影响 STRONG_EVIDENCE / clean_data / readiness
下一步先做 dependency audit
```

大白话：现在已经设计好真实 PDF 文本层 provider 的边界，但还没有开始实现，也没有新增依赖或跑真实 PDF。

---

## 3. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
已经能跑真实 PDF
已经能批量处理客户 PDF
```

R7AI 的正确含义是：真实 PDF 文本层 provider 的安全边界设计通过，下一步先查依赖能不能安全用。

---

## 4. 当前下一步：R7AJ

当前下一步：

```text
348N-R7AJ dependency audit for real PDF text-layer provider
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = dependency-audit-only
```

R7AJ 要做：

```text
盘点 pyproject / requirements / setup / lock files
确认是否已有 PyMuPDF / pdfplumber / pypdf / pdfminer.six 等 PDF 文本库
比较 pypdf / PyMuPDF / pdfplumber / pdfminer.six / no-new-dependency 的风险
判断是否能直接做 test-only provider implementation
如果没有合适依赖，推荐 optional dependency design 或 adapter-boundary-only skeleton
继续保持 MinerU/OCR fallback，不作为默认入口
```

R7AJ 不做：

```text
新增依赖
实现 PDF parser
跑真实 PDF
跑 MinerU / OCR / LLM / VLM
接 production path
production readiness
```

---

## 5. 一句话总结

```text
项目已经从 synthetic page-text bridge 走到真实 PDF text-layer provider 的设计阶段；下一步 R7AJ 先做依赖审计，防止为了接真实 PDF 直接把项目拖进重依赖和批量解析泥潭。
```
