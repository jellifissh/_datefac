# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线不是重新抽 PDF，也不是训练模型。

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. clean-boundary 到哪里了

```text
Anjing / 安井：R7U clean_data = 65，review_queue = 17
Linyang / 林洋：R7U clean_data = 0，review_queue = 489，unknown = 0
Taihao / 泰豪：R7T clean_data 92 -> 72，review_queue 66 -> 86
```

R7P 到 R7V 的价值是：

```text
MARKET_REFERENCE_ROW 不再进入 clean_data
strict-table scaffolding / pseudo-header / comparison 行被移入 review_queue
readiness gates 继续关闭
```

---

## 3. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
```

R7AA 结论：

```text
当前 active pipeline 没有 trusted production source_text carrier
Excel 里的来源页、页码、摘录等字段不能直接当 trusted source_text
source_text 应该通过 sidecar/index 接入，并绑定 source_id / page_number / locator
source_text 缺失、不可信、source/page 不匹配时保持 UNVERIFIED
不默认在 evidence_index / review_queue 存全文
```

---

## 4. 当前不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

原因：source_text 还没有真实接入 pipeline，row-level matching 也还不是 period-aware / coordinate-aware。

---

## 5. 当前下一步：R7AB

当前下一步：

```text
348N-R7AB source_text availability / evidence index wiring implementation
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AB 是 source_text metadata 和 checker 注入的第一段实现，必须保持默认 UNVERIFIED，不得改变 clean/readiness 边界。
```

R7AB 要做的是最小实现：

```text
source_text sidecar/index contract
trusted source_text selection
checker-call-time source_text injection
evidence_index metadata
必要时 review_queue compact fields
```

R7AB 不能做：

```text
运行 MinerU / OCR / LLM / VLM
把 Excel raw fields 当 trusted source_text
默认存全文
打开 readiness gates
让 VERIFIED 自动变成 STRONG_EVIDENCE 或 clean admission
```

---

## 6. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7AA 已经设计好 source_text 的安全接入契约，下一步 R7AB 才开始做最小实现。
```
