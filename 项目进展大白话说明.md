# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
```

R7AD-QA 确认：

```text
R7AD 是 tests-only
使用 in-test SourceTextEvidence，不是 file-backed loader
trusted fixture 可以 VERIFIED
trusted mismatch 可以 DISAGREED
missing / source_id mismatch / page mismatch / locator mismatch / untrusted / empty 都保持 UNVERIFIED
evidence_index metadata-only，没有 full source_text
review_queue compact fields，没有 full source_text
VERIFIED 不影响 STRONG_EVIDENCE / clean admission / readiness
pytest tests/agent -q = 134 passed
```

大白话：source_text 的内存级 dry-run 已经站住了，下一步才考虑从文件读取 sidecar。

---

## 3. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7AD-QA 的正确含义是：fixture dry-run 有效，不是系统已经可以正式交付。

---

## 4. 当前下一步：R7AE

当前下一步：

```text
348N-R7AE source_text file-backed sidecar loader design
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AE 要设计 file-backed source_text sidecar loader；loader contract 如果不保守，会把 fixture-only evidence 误扩展成不安全的生产证据。
```

R7AE 要设计：

```text
sidecar 用 JSON 还是 JSONL
sidecar schema 和 required/optional fields
fixture 文件放哪里
loader 是 test-only 还是 runner 可见
malformed / mismatch / untrusted / empty 如何 fail closed
text_sha256 如何计算和校验
如何保证 evidence_index / review_queue 不写 full source_text
下一步 R7AF implementation slice 怎么切
```

R7AE 不是：

```text
实现 loader
真实 workbook rerun
MinerU / OCR / LLM / VLM
PDF extraction
production readiness
```

---

## 5. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7AD-QA 确认内存 fixture dry-run 有效，下一步 R7AE 要设计 file-backed sidecar loader。
```
