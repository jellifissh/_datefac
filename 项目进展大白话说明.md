# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
R7AE: file-backed source_text sidecar loader 设计完成
```

R7AE 结论：

```text
sidecar format = JSON object v1
fixture_scope = test_only
records[] 严格 schema
no unknown keys
fixture location = tests/agent/fixtures/source_text_sidecars/
loader placement = test-only first
malformed / unsafe / hash mismatch / duplicate / unsupported fields fail closed
full source_text 只允许在 test fixture 内存在，不能写入 evidence_index / review_queue
```

大白话：文件型 source_text sidecar 的规矩已经定好了，下一步 R7AF 可以一次性做大一点：loader + fixture + tests + self-QA。

---

## 3. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7AE 的正确含义是：file-backed sidecar loader 的设计通过，不是系统已经可以正式交付。

---

## 4. 当前下一步：R7AF

当前下一步：

```text
348N-R7AF test-only source_text file-backed sidecar loader implementation
```

任务大小与执行方式：

```text
task_size = large
recommended_reasoning_level = max
execution_mode = phased-hard-stop + implementation-with-self-QA
```

R7AF 要做：

```text
实现 test-only JSON sidecar loader
创建 tests/agent/fixtures/source_text_sidecars/ 下的 JSON fixture
覆盖 valid loading
覆盖 malformed / unknown key / duplicate / bad hash / bad char_count / untrusted 等 fail-closed cases
复用现有 source_text wiring 验证 VERIFIED / DISAGREED / UNVERIFIED
用 tempfile 验证 evidence_index metadata-only
用 in-memory 验证 review_queue compact fields
确认 full source_text 不进入 serialized outputs
```

R7AF 不做：

```text
production loader
CLI / runner defaults
真实 workbook rerun
MinerU / OCR / LLM / VLM
PDF extraction
production readiness
```

---

## 5. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7AE 设计好了文件型 sidecar 的安全规则，R7AF 要用一个大任务把 test-only loader、fixture、测试和自检一起做完。
```
