# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

## 当前项目在做什么

```text
把已有抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

## 最新进展

```text
R7AN：test-only MinerU adapter controlled comparison dry-run 设计完成
```

关键结果：

```text
commit = f29cc07 docs: add R7AN controlled comparison design
input_scope = 完整安井 DateFac Excel + 完整本地 MinerU content_list_v2
runner_scope = 本地 output/comparison/anjing_foods_mineru_adapter_r7ao/
adapter_usage = 直接复用 tests.agent.mineru_artifact_adapter_348n
output_policy = 只写本地 xlsx/csv/md/json，不提交 outputs
测试 = 21 passed / 201 passed
readiness_gates = CLOSED
```

这说明：

```text
下一步可以把 R7AM 的 test-only adapter 用在完整安井样本 controlled comparison 上。
```

但仍然不代表：

```text
已经接入 production pipeline
可以批量处理客户 PDF
VERIFIED 可以自动进入 clean_data
VERIFIED 可以升级为 STRONG_EVIDENCE
production_ready = true
```

## 当前下一步

```text
348N-R7AO test-only MinerU adapter controlled comparison runner
```

R7AO 要做：

```text
在本地 output 目录写 runner，读取完整安井 DateFac Excel 和完整 MinerU content_list_v2，复用 R7AM adapter，生成本地 comparison 报告。
```

R7AO 不做：

```text
不改 production code
不改 tests
不提交 runner
不提交 xlsx/csv/md/json outputs
不跑 MinerU
不跑 OCR / LLM / VLM
不新增依赖
不打开 readiness gates
不 commit / 不 push
```

一句话：

```text
现在进入完整本地 dry-run，但仍然是 no-commit、no-production 的受控实验。
```
