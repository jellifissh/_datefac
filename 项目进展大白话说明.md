# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线不是重新抽 PDF，也不是训练模型。

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心流程：

```text
intake -> audit -> review -> delivery
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. 当前验证过的 workbook family

```text
Anjing / 安井：R7U clean_data = 65，review_queue = 17
Linyang / 林洋：R7U clean_data = 0，review_queue = 489，unknown = 0
Taihao / 泰豪：R7T clean_data 92 -> 72，review_queue 66 -> 86
```

Taihao R7T 的关键价值是：20 个 strict-table scaffolding / pseudo-header / comparison 行从 clean_data 移到 review_queue。

---

## 3. R7P 到 R7V：clean-boundary 收束

R7P 暴露的问题：

```text
MARKET_REFERENCE_ROW 里的“收盘价”等行进入了 clean_data
```

R7P-FIX2 修正：

```text
MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
```

R7S 又收紧 strict-table：

```text
STRICT_FINANCIAL_TABLE_ROW + WEAK_EVIDENCE + scaffolding-like shape
-> REVIEW_REQUIRED
```

R7V 总结：

```text
Taihao / Linyang / Anjing 跨 family clean-boundary 验证通过
readiness gates 继续关闭
```

---

## 4. R7W / R7X / R7X-QA：证据 provenance 起步

R7W 的核心原则：

```text
Strong evidence must mean traceable and auditable, not merely plausible.
```

R7X 做了：

```text
page_number parsing
agreement_status 字段
```

R7X-QA 确认：

```text
page_number parsed != VERIFIED
UNVERIFIED != STRONG_EVIDENCE
readiness_gates = closed
tests/agent = 95 passed
```

大白话：知道该去哪一页找证据，不等于已经核验证据。

---

## 5. R7Y / R7Y-QA：数值一致性检查器

R7Y 实现 deterministic source-value agreement checker。

R7Y 结果：

```text
commit = 973ff68 feat: add deterministic evidence agreement checker
pytest tests/agent -q = 106 passed
```

R7Y 语义：

```text
no explicit/page provenance -> MISSING
explicit/page provenance but no source_text -> UNVERIFIED
all row numeric period_values match source_text -> VERIFIED
source_text has numbers but no row numeric values match -> DISAGREED
partial coverage -> UNVERIFIED
text-only facts -> UNVERIFIED
```

R7Y-QA 已确认：

```text
commit = 4e71f28 docs: add R7Y QA review
Decision = PASS，R7Y deterministic source-value agreement checker QA valid
test_result = pytest tests/agent -q => 106 passed
qa_result = VALID
readiness_gates = CLOSED
```

关键边界：

```text
VERIFIED != STRONG_EVIDENCE
VERIFIED != clean admission
VERIFIED != production_ready
```

---

## 6. R7Y-QA 发现的后续风险

R7Y-QA 通过，但指出两个后续精度风险：

```text
row-level token matching is not yet period-aware or coordinate-aware
duplicate numeric values are matched against a source token set
```

这不是 R7Y failure，但在接入真实证据输入前应该继续加固测试。

---

## 7. 当前下一步：R7Z

当前下一步：

```text
348N-R7Z agreement checker edge-case fixture coverage
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7Z 针对 VERIFIED / DISAGREED 的边界用例加固，错误通过会导致后续证据强度误判。
```

R7Z 要补的重点：

```text
duplicate numeric values
partial multi-period coverage
source text with unrelated numeric tokens
source text with no numeric tokens
text-only facts
```

R7Z 不是 source-text wiring，也不是 production readiness。

---

## 8. 当前状态一句话

```text
项目已经把 clean_data 边界推进到跨 workbook family 更保守、更可解释，并把证据链推进到 page provenance + deterministic numeric agreement checker；下一步要加固 agreement checker 的边界用例，防止 VERIFIED 误判。
```

仍然必须记住：

```text
page_number parsed != VERIFIED
UNVERIFIED != STRONG_EVIDENCE
VERIFIED != production_ready
clean_data 更干净 != 正式交付
```
