# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

## 当前项目在做什么

```text
把已有抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

## 最新进展

```text
R7AM-QA：test-only MinerU artifact adapter prototype 审查通过
```

关键结果：

```text
commit = 4daa3bf docs: add R7AM QA review
单文件测试 = 21 passed
agent 测试集 = 201 passed
fixture = 小型 curated，2599 bytes / 7 blocks
adapter = 只在 tests/agent/，没有 production hook
matching helper = 保守区分 VERIFIED / UNVERIFIED / DISAGREED / AMBIGUOUS / MISSING_EVIDENCE
未改 datefac_agent/
未提交完整 MinerU output
未提交 DateFac Excel
未提交 comparison output
未新增依赖
未跑 MinerU / OCR / LLM / VLM
readiness_gates = CLOSED
```

这说明：

```text
R7AM 的小型 MinerU adapter 原型可以作为下一步 dry-run 的测试基础。
```

它仍然不代表：

```text
已经接入生产 pipeline
可以批量处理客户 PDF
VERIFIED 可以自动进入 clean_data
VERIFIED 可以升级为 STRONG_EVIDENCE
production_ready = true
```

## 当前下一步

```text
348N-R7AN test-only MinerU adapter controlled comparison dry-run design
```

R7AN 要做：

```text
先设计怎么用 R7AM 的 test-only adapter 形状，执行下一轮 controlled comparison dry-run。
```

R7AN 不做：

```text
不写 runner
不改 tests
不改 production code
不提交 output
不跑 MinerU
不跑 OCR / LLM / VLM
不新增依赖
不打开 readiness gates
```

一句话：

```text
adapter 原型已经通过 QA；下一步先设计 dry-run 怎么跑，别直接冲生产。
```
