# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

## 当前项目在做什么

```text
把已有抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

## 最新进展

```text
R7AO-QA：本地 controlled comparison runner 审查通过
```

关键结果：

```text
commit = 6e02622 docs: add R7AO QA review
451 = 402 VERIFIED + 49 non-VERIFIED
11 个探针样例全部 VERIFIED
runner = local-only，复用 tests.agent.mineru_artifact_adapter_348n
metadata = counts / inputs / branch / head / readiness 已记录
outputs = xlsx / md / json / csv complete
CSV = metadata-only evidence，没有失控 dumping
未改 production code
未改 tests
未提交 output
未新增依赖
未跑 MinerU / OCR / LLM / VLM
readiness_gates = CLOSED
```

这说明：

```text
完整安井样本的本地 dry-run 已经通过 QA，adapter 路线在单文档上可信。
```

但仍然不代表：

```text
已经接入 production pipeline
可以批量处理客户 PDF
VERIFIED 可以自动进入 clean_data
VERIFIED 可以升级为 STRONG_EVIDENCE
production_ready = true
```

## 当前下一步

```text
348N-R7AP DateFac-MinerU discrepancy review workflow design
```

R7AP 要做：

```text
设计 49 条 non-VERIFIED rows 怎么进入复核流程。
```

重点是：

```text
DISAGREED 怎么处理
AMBIGUOUS 怎么处理
MISSING_EVIDENCE 怎么处理
PARSE_SKIPPED 怎么处理
review_queue item 需要哪些字段
人工 reviewer 能做哪些动作
review 后怎样才允许进入 clean_data
未解决问题如何阻断导出或标注导出
```

R7AP 不做：

```text
不写 implementation
不改 production code
不改 tests
不提交 output
不跑 MinerU / OCR / LLM / VLM
不新增依赖
不打开 readiness gates
```

一句话：

```text
现在不是急着接生产，而是先把“有问题的 49 行怎么复核”设计清楚。
```
