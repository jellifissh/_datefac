# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
R7AE: file-backed source_text sidecar loader 设计完成
R7AF: test-only file-backed source_text sidecar loader 实现完成
R7AF-QA: file-backed source_text sidecar loader 审查通过
R7AG: lightweight PDF evidence bridge 设计通过
R7AH: lightweight PDF evidence bridge test-only 原型完成
```

R7AH 做了：

```text
新增 tests/agent/lightweight_pdf_evidence_bridge_348n.py
新增 tests/agent/test_lightweight_pdf_evidence_bridge_348n.py
使用 synthetic page-text provider，不跑真实 PDF
实现 target-page lookup 和 capped candidate search
要求 value + metric + period proximity 才生成 SourceTextEvidence
value-only / no-value / wrong-page / duplicate ambiguous / missing text / scanned marker 都保守失败
验证 accepted snippet 可驱动 VERIFIED
验证 evidence_index / review_queue 不写 full snippet text
验证无 PyMuPDF/pdfplumber/pypdf/MinerU/OCR/LLM/VLM/production hook
```

R7AH 结果：

```text
commit = 26e2717 test: add lightweight PDF evidence bridge prototype
pytest tests/agent -q = 180 passed
readiness_gates = CLOSED
```

大白话：现在已经证明，在测试环境里，不用 MinerU，也可以用“产物里的页码/指标/期间/数值 + PDF 页文本锚点”生成 SourceTextEvidence。

---

## 3. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
已经能跑真实 PDF
```

R7AH 的正确含义是：轻量 evidence bridge 的测试原型可行，不是真实 PDF provider 已经接入。

---

## 4. 当前下一步：R7AH-QA

当前下一步：

```text
348N-R7AH-QA lightweight PDF evidence bridge prototype review
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = QA-review-only
```

R7AH-QA 要审：

```text
prototype 是否只在 tests/agent/
是否没有 datefac_agent/ production code
是否没有真实 PDF parser
是否没有 PyMuPDF/pdfplumber/pypdf 依赖
是否没有 MinerU/OCR/LLM/VLM
是否使用 synthetic provider
是否要求 value + metric + period proximity
保守失败 cases 是否都失败
SourceTextEvidence 映射是否正确
evidence_index / review_queue 是否不序列化 full source_text
VERIFIED 是否不影响 STRONG_EVIDENCE / clean_data / readiness
```

---

## 5. 一句话总结

```text
项目已经从“重型解析器默认入口”转成“轻量证据桥优先”；R7AH 用测试原型证明这条低成本路线可走，下一步 R7AH-QA 要确认它没有越界。
```
