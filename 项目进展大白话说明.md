# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

## 当前项目在做什么

```text
把已有抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

## 最新进展

```text
R7AJ dependency audit 已完成
```

R7AJ 的结论很直接：当前项目没有声明任何 PDF 文本层库。

```text
没有 PyMuPDF / fitz
没有 pdfplumber
没有 pypdf / PyPDF2
没有 pdfminer.six
没有 reportlab
```

所以不能直接开始写真实 PDF provider。下一步要先设计 optional dependency 怎么加，避免项目被依赖安装坑、重型解析器、批量 PDF 平台拖偏。

## 当前下一步

```text
348N-R7AK optional PDF dependency addition design
```

R7AK 要做：

```text
比较 pypdf / PyMuPDF / pdfplumber / pdfminer.six
选第一候选 PDF 文本层库
设计 optional import
设计 dependency missing status
设计依赖添加任务和 QA gates
决定 dependency addition 和 provider implementation 是否拆开
继续保持 MinerU/OCR fallback-only
```

R7AK 不做：

```text
不新增依赖
不 pip install
不实现 PDF parser
不跑真实 PDF
不接 MinerU/OCR/LLM/VLM
不打开 readiness
```

一句话：

```text
接下来不是写 PDF 解析代码，而是先安全选库，防止项目为了接真实 PDF 一脚踩进重依赖和批量解析泥潭。
```
