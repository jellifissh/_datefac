# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
R7AE: file-backed source_text sidecar loader 设计完成
R7AF: test-only file-backed source_text sidecar loader 实现完成
R7AF-QA: file-backed source_text sidecar loader 审查通过
R7AG: lightweight PDF evidence bridge 设计通过
R7AH: lightweight PDF evidence bridge test-only 原型完成
R7AH-QA: lightweight PDF evidence bridge 原型审查通过
```

R7AH-QA 确认：

```text
prototype 只在 tests/agent/
只使用 synthetic page-text provider
没有真实 PDF IO
没有 PyMuPDF/pdfplumber/pypdf imports
没有新增依赖
没有 MinerU/OCR/LLM/VLM
没有 production hook
要求 value + metric + period proximity
保守失败 cases 均保守失败
SourceTextEvidence 映射正确
evidence_index / review_queue 不写 full snippet text
VERIFIED 不影响 STRONG_EVIDENCE / clean_data / readiness
pytest tests/agent -q = 180 passed
```

大白话：轻量证据桥测试原型已经安全通过 QA。它证明“低成本路线可走”，但还没有接真实 PDF 文本层。

---

## 3. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
已经能跑真实 PDF
已经能批量处理客户 PDF
```

R7AH-QA 的正确含义是：测试原型 valid，不是真实 PDF provider valid。

---

## 4. 当前下一步：R7AI

当前下一步：

```text
348N-R7AI real PDF text-layer provider design
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = design-review-only
```

R7AI 要设计：

```text
真实 PDF text-layer provider 的接口
是否复用 R7AH synthetic provider API
依赖策略：PyMuPDF / pdfplumber / pypdf / optional dependency / 先 dependency audit
如何检测 text layer
如何识别 scanned/image-only PDF
有 page_number 时如何只抽目标页
没 page_number 时如何 capped candidate search
如何缓存 source_file_sha256 / page_text / provider metadata / extraction config
如何防止 stale cache
如何映射 SourceTextEvidence
trusted_source / extraction_method / text_kind 怎么定义
哪些结果能支持 VERIFIED，但仍不影响 STRONG_EVIDENCE / clean_data / readiness
什么时候 fallback 到 MinerU / OCR / manual review
R7AJ 下一步是 implementation 还是 dependency audit
```

R7AI 不做：

```text
实现真实 PDF parser
新增依赖
跑真实 PDF
跑 MinerU / OCR / LLM / VLM
接 runner / CLI / production path
production readiness
```

---

## 5. 一句话总结

```text
项目已经证明轻量证据桥测试原型有效；下一步 R7AI 只设计真实 PDF 文本层 provider 怎么接，继续避免一上来变成 MinerU-first 或批量 PDF 平台。
```
