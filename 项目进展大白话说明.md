# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线不是重新抽 PDF，也不是训练模型。

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. clean-boundary 已经收束到什么程度

目前已经验证过：

```text
Anjing / 安井：R7U clean_data = 65，review_queue = 17
Linyang / 林洋：R7U clean_data = 0，review_queue = 489，unknown = 0
Taihao / 泰豪：R7T clean_data 92 -> 72，review_queue 66 -> 86
```

R7P 到 R7V 的价值是：

```text
MARKET_REFERENCE_ROW 不再进入 clean_data
strict-table scaffolding / pseudo-header / comparison 行被移入 review_queue
跨 workbook family 没有发现 R7S 回归
readiness gates 继续关闭
```

---

## 3. evidence strengthening 走到哪里了

R7W 定的原则：

```text
Strong evidence must mean traceable and auditable, not merely plausible.
```

R7X 做了：

```text
page_number parsing
agreement_status 字段
```

R7X-QA 确认：

```text
page_number parsed != VERIFIED
UNVERIFIED != STRONG_EVIDENCE
```

R7Y 做了 deterministic source-value agreement checker：

```text
all row numeric period_values match source_text -> VERIFIED
source_text has numbers but no row numeric values match -> DISAGREED
partial coverage -> UNVERIFIED
text-only facts -> UNVERIFIED
```

R7Y-QA 确认：

```text
VERIFIED != STRONG_EVIDENCE
VERIFIED != clean admission
VERIFIED != production_ready
```

---

## 4. R7Z 解决了什么

R7Z 不是接真实 source_text pipeline，而是加固 agreement checker 的边界用例。

R7Y-QA 发现一个精度风险：

```text
duplicate numeric values are matched against a source token set
```

大白话：如果 Excel 一行里两个期间都是 100，但 source_text 里只有一个 100，旧逻辑可能因为 set membership 把它误判成 VERIFIED。

R7Z 复现并修复了这个问题：

```text
row period_values = {2024A: 100, 2025A: 100}
source_text = one occurrence of 100
before -> VERIFIED
after -> UNVERIFIED
```

R7Z 改成：

```text
source numeric tokens are counted with Counter[Decimal]
each row numeric value consumes one available source occurrence
duplicate row values require duplicate source occurrences
```

R7Z 结果：

```text
commit = 004e307 fix: make agreement checker multiplicity conservative
pytest tests/agent -q = 111 passed
verified_false_positive_result = REDUCED
readiness_gates = CLOSED
```

这一步很关键：不是让系统更大胆，而是让系统在边界场景下更不容易瞎喊 VERIFIED。

---

## 5. 当前还不能吹什么

当前仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7Z 仍然没有解决：

```text
row-level matching is still not period-aware or coordinate-aware
```

也就是说，它能防止一类重复数字误判，但还没有真正理解数字在表格里的坐标、期间、上下文。

---

## 6. 当前下一步：R7Z-QA

当前下一步：

```text
348N-R7Z-QA agreement checker edge-case review
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7Z-QA 要确认 multiplicity-aware fix 真正降低 VERIFIED 误判，同时没有让 DISAGREED 过激、没有改变 clean/readiness 边界。
```

R7Z-QA 要审：

```text
duplicate row numeric values 是否需要 duplicate source occurrences
partial coverage 是否仍为 UNVERIFIED
source_text 无数字是否仍为 UNVERIFIED
text-only facts 是否仍为 UNVERIFIED
DISAGREED 是否仍只在 full mismatch 时产生
VERIFIED 是否没有自动变成 STRONG_EVIDENCE
VERIFIED 是否没有自动进入 clean_data
readiness gates 是否仍关闭
```

R7Z-QA 通过后，再考虑 source_text integration design / evidence index wiring。

---

## 7. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7Z 把 agreement checker 从简单 set 匹配加固为 multiplicity-aware 匹配，降低了 duplicate numeric values 导致的 VERIFIED 误判风险。
```

仍然必须记住：

```text
page_number parsed != VERIFIED
UNVERIFIED != STRONG_EVIDENCE
VERIFIED != production_ready
clean_data 更干净 != 正式交付
```
