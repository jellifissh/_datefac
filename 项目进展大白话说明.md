# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. clean-boundary 到哪里了

```text
Anjing / 安井：R7U clean_data = 65，review_queue = 17
Linyang / 林洋：R7U clean_data = 0，review_queue = 489，unknown = 0
Taihao / 泰豪：R7T clean_data 92 -> 72，review_queue 66 -> 86
```

R7P 到 R7V 已经把 market reference、伪表头、comparison/scaffolding 行从 clean_data 边界里收紧出来，readiness gates 仍然关闭。

---

## 3. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
```

R7AB-QA 确认：

```text
SourceTextEvidence / SourceTextSelection contract = PASS
trusted source_text selection = PASS
checker-call-time injection = PASS
evidence_index metadata = PASS, metadata/hash only, no full source_text
review_queue compact fields = PASS, no full source_text
VERIFIED does not affect STRONG_EVIDENCE, clean admission, or readiness
pytest tests/agent -q = 123 passed
```

大白话：source_text 这根线已经接上了，但它仍然必须经过 source_id / page_number / locator / trust / non-empty 等条件，不能随便拿 Excel 里的文字冒充原文证据。

---

## 4. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7AB-QA 的正确含义是：source_text wiring 这段接线通过审查，不是系统可以正式交付。

---

## 5. 当前下一步：R7AC

当前下一步：

```text
348N-R7AC source_text sidecar fixture integration / dry-run design
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AC 要设计如何用 fixture sidecar / dry-run 安全测试 R7AB source_text wiring，避免真实 pipeline 接入前出现 VERIFIED 误判或边界外泄。
```

R7AC 是设计任务，要回答：

```text
fixture sidecar 用 JSON、JSONL 还是 test object
fixture sidecar 放哪里
是否 commit fixture 或只在测试中生成
最小 positive / negative cases 是什么
evidence_index metadata 怎么验证
review_queue compact fields 怎么验证
下一步 implementation slice 怎么切
```

R7AC 不是：

```text
运行 workbook rerun
运行 MinerU / OCR / LLM / VLM
接真实 PDF extraction pipeline
打开 readiness gates
```

---

## 6. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7AB-QA 确认 source_text wiring 安全，下一步 R7AC 要设计 fixture/dry-run 路径，让这段 wiring 能在受控样例里被验证。
```
