# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

## 当前项目在做什么

```text
把已有抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

## 最新进展

```text
R7AO：test-only MinerU adapter controlled comparison runner 已本地跑通
```

关键结果：

```text
DateFac 候选 rows = 451
MinerU adapter blocks = 89
VERIFIED = 402
需复核 = 49
DISAGREED = 5
AMBIGUOUS = 10
MISSING_EVIDENCE = 1
PARSE_SKIPPED = 18
11 个探针样例全部 VERIFIED
测试 = 21 passed / 201 passed
commit = none
push = none
readiness_gates = CLOSED
```

这说明：

```text
R7AM 的 test-only adapter 形状已经能支撑完整安井样本 controlled comparison。
```

但仍然不代表：

```text
已经接入 production pipeline
可以批量处理客户 PDF
VERIFIED 可以自动进入 clean_data
VERIFIED 可以升级为 STRONG_EVIDENCE
production_ready = true
```

本地输出在：

```text
D:\_datefac_agent\output\comparison\anjing_foods_mineru_adapter_r7ao\
```

这些 output 不提交。

## 当前下一步

```text
348N-R7AO-QA test-only MinerU adapter controlled comparison runner review
```

R7AO-QA 要做：

```text
审查 R7AO 本地 runner 和输出报告是否可信：
- 计数是否一致
- 11 个探针是否全部覆盖
- output 是否只在本地目录
- runner 是否复用 R7AM adapter
- 是否没有 production hook
- 是否没有提交 output
- 是否没有修改 datefac_agent/ 或 tests/
- VERIFIED 是否仍然不等于 STRONG_EVIDENCE / clean_data admission
```

一句话：

```text
完整本地 dry-run 已跑通；下一步先 QA，再决定是做生产边界设计、多文档 dry-run，还是专门设计差异复核工作流。
```
