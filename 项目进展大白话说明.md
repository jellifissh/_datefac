# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

## 当前项目在做什么

```text
把已有抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

## 最新进展

```text
R7AM：test-only MinerU artifact adapter prototype 已完成
```

关键结果：

```text
commit = 0de3a4a test: add MinerU artifact adapter prototype
单文件测试 = 21 passed
agent 测试集 = 201 passed
小型 curated fixture 已提交
完整 MinerU output 未提交
DateFac comparison output 未提交
生产代码未修改
readiness_gates = CLOSED
```

R7AM 做成了什么：

```text
把 R7AL 真实对比里证明可行的 MinerU content_list_v2 证据结构，整理成 tests/agent/ 下的小型可重复原型。
```

它包括：

```text
1. test-only MinerU content_list_v2 adapter
2. 小型安井食品 fixture
3. matching helper
4. VERIFIED / UNVERIFIED / DISAGREED / AMBIGUOUS / MISSING_EVIDENCE 测试覆盖
```

它不代表：

```text
已经接入生产 pipeline
已经可以批量处理客户 PDF
VERIFIED 可以自动进 clean_data
VERIFIED 可以升级为 STRONG_EVIDENCE
production_ready = true
```

## 当前下一步

```text
348N-R7AM-QA test-only MinerU artifact adapter prototype review
```

R7AM-QA 要做：

```text
审查 R7AM 是否真的保持 test-only
确认 fixture 是小型 curated，不是完整 MinerU dump
确认 adapter 没有生产接入
确认 matching helper 足够保守
确认 value-only / metric-only 不会 VERIFIED
确认没有提交 output/comparison 文件
确认 readiness gates 仍然关闭
```

一句话：

```text
真实对比已经跑通，小型 adapter 原型也写出来了；下一步必须 QA，防止这个原型越界成“伪生产能力”。
```
