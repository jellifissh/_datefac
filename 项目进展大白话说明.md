# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. clean-boundary 到哪里了

```text
Anjing / 安井：R7U clean_data = 65，review_queue = 17
Linyang / 林洋：R7U clean_data = 0，review_queue = 489，unknown = 0
Taihao / 泰豪：R7T clean_data 92 -> 72，review_queue 66 -> 86
```

R7P 到 R7V 已经把 market reference、伪表头、comparison/scaffolding 行从 clean_data 边界里收紧出来，readiness gates 仍然关闭。

---

## 3. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
```

R7AB 做了：

```text
SourceTextEvidence / SourceTextSelection
trusted source_text selection
checker-call-time source_text injection
evidence_index source_text metadata
review_queue compact fields
```

R7AB 结果：

```text
commit = 12a4726 feat: wire trusted source text metadata
pytest tests/agent -q = 123 passed
readiness_gates = CLOSED
```

---

## 4. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7AB 的正确含义是：系统有了 trusted source_text 的最小接线方式，不是已经可以正式交付。

---

## 5. 当前下一步：R7AB-QA

当前下一步：

```text
348N-R7AB-QA source_text availability / evidence index wiring review
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AB-QA 要确认 trusted source_text selection、checker injection、evidence_index metadata、review_queue compact fields 均未越过 clean/readiness 边界。
```

R7AB-QA 要审：

```text
source_text contract 是否足够清楚
source_id/page_number/locator mismatch 是否保持 UNVERIFIED
untrusted / empty source_text 是否保持 UNVERIFIED
full source_text 是否没有默认序列化
review_queue compact fields 是否安全
VERIFIED 是否没有进入 STRONG_EVIDENCE / clean admission / readiness
```

---

## 6. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7AB 已经完成 source_text 最小安全接线，下一步要做 R7AB-QA 审查这段接线是否真的保守可靠。
```
