# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. clean-boundary 到哪里了

```text
Anjing / 安井：R7U clean_data = 65，review_queue = 17
Linyang / 林洋：R7U clean_data = 0，review_queue = 489，unknown = 0
Taihao / 泰豪：R7T clean_data 92 -> 72，review_queue 66 -> 86
```

R7P 到 R7V 已经把 market reference、伪表头、comparison/scaffolding 行从 clean_data 边界里收紧出来，readiness gates 仍然关闭。

---

## 3. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
```

R7AD 做了：

```text
用 in-test SourceTextEvidence objects 做 dry-run tests
用 tempfile 验证 evidence_index metadata
用 in-memory 验证 review_queue compact fields
覆盖 trusted verify / trusted mismatch DISAGREED
覆盖 missing / source_id mismatch / page mismatch / locator mismatch / untrusted / empty -> UNVERIFIED
确认 full source_text 不进入 serialized outputs
```

R7AD 结果：

```text
commit = 9cd4ef6 test: add source text fixture dry-run coverage
pytest tests/agent -q = 134 passed
readiness_gates = CLOSED
```

大白话：现在 source_text 这根线不只是接上了，还用受控样例证明了“该用时能用、不该用时拒绝、输出不泄露全文”。

---

## 4. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7AD 的正确含义是：fixture dry-run 覆盖有效，不是已经可以正式交付。

---

## 5. 当前下一步：R7AD-QA

当前下一步：

```text
348N-R7AD-QA source_text fixture dry-run review
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AD-QA 要确认 fixture dry-run 真实覆盖 source_text selection、evidence_index metadata、review_queue compact fields、全文不序列化和 clean/readiness 边界。
```

R7AD-QA 要审：

```text
R7AD 是否 tests-only
是否使用 in-test SourceTextEvidence，而不是 file loader
是否没有跑 workbook rerun / MinerU / OCR / LLM / VLM
positive / negative cases 是否覆盖充分
evidence_index 是否 metadata-only 且无 full source_text
review_queue 是否 compact fields 且无 full source_text
VERIFIED 是否没有影响 STRONG_EVIDENCE / clean admission / readiness
```

---

## 6. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7AD 已经用受控 fixture dry-run 证明 source_text wiring 的正负路径和输出边界，下一步做 R7AD-QA。
```
