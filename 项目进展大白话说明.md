# 项目进展大白话说明

这份文档用于快速说明 DateFac Agent 当前进展。

---

## 1. 当前项目在做什么

当前主线是：

```text
把已有 Excel 抽取结果读进来，判断哪些行可信，哪些行必须人工复核，最后形成可审计的 clean_data 和 review_queue。
```

核心原则：

```text
宁可让数据进 review_queue，也不要把不确定的数据错误放进 clean_data。
```

---

## 2. evidence strengthening 到哪里了

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字导致的 VERIFIED 误判
R7AA: source_text integration / evidence index wiring 设计完成
R7AB: source_text metadata 和 checker 注入的最小实现完成
R7AB-QA: source_text wiring 审查通过
R7AC: source_text sidecar fixture / dry-run 设计完成
R7AD: source_text fixture dry-run 测试覆盖完成
R7AD-QA: fixture dry-run 审查通过
R7AE: file-backed source_text sidecar loader 设计完成
R7AF: test-only file-backed source_text sidecar loader 实现完成
```

R7AF 做了：

```text
新增 tests/agent/source_text_sidecar_loader_348n.py
新增 tests/agent/test_source_text_sidecar_loader_348n.py
新增 tests/agent/fixtures/source_text_sidecars/r7af_source_text_sidecar__basic_positive_negative__v1.json
JSON object v1 loader
严格 schema keys
UTF-8 sha256 校验
char_count 校验
trusted_source=true 校验
fail-closed invalid cases
valid fixture 驱动 VERIFIED / DISAGREED / UNVERIFIED
确认 evidence_index / review_queue 不写 full source_text
```

R7AF 结果：

```text
commit = 3285ca6 test: add source text sidecar loader coverage
pytest tests/agent -q = 162 passed
readiness_gates = CLOSED
```

大白话：文件型 source_text sidecar 已经能在测试环境里读进来，而且坏文件会整包拒绝，不会半信半疑地加载一部分。

---

## 3. 当前仍然不能吹什么

仍然不能说：

```text
production_ready = true
formal_client_export_allowed = true
VERIFIED = STRONG_EVIDENCE
VERIFIED = clean admission
```

R7AF 的正确含义是：test-only file-backed loader 可用，不是 production loader 已经接入。

---

## 4. 当前下一步：R7AF-QA

当前下一步：

```text
348N-R7AF-QA source_text file-backed sidecar loader review
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = QA-review-only
```

R7AF-QA 要审：

```text
loader 是否只在 tests/agent/
是否没有 datefac_agent / runner / CLI / run_pilot hook
JSON schema 是否严格
malformed / unknown / missing / duplicate / bad hash / bad count / untrusted 是否 fail closed
是否没有 partial records
valid fixture 是否映射 SourceTextEvidence
是否驱动 VERIFIED / DISAGREED / UNVERIFIED
是否 metadata-only 且不序列化 full source_text
VERIFIED 是否没有影响 STRONG_EVIDENCE / clean admission / readiness
```

---

## 5. 一句话总结

```text
项目已经从 clean_data 边界治理推进到证据链治理；R7AF 已实现 test-only file-backed sidecar loader，下一步 R7AF-QA 确认这东西没有越界成生产证据入口。
```
