# PROJECT_MILESTONE_LEDGER moved / 项目进程账本已迁移

中文：
当前 DateFac Agent 主线的项目进程事实源已经迁移到：

```text
docs/agent/项目进程.md
```

旧路径：

```text
docs/project_milestones/PROJECT_MILESTONE_LEDGER_项目进程.md
```

已降级为历史归档入口，不再作为 348 Agent / 348S 任务的默认必读文档。

后续新的聊天、新模型和新的 Codex 线程，应优先读取 `docs/agent/项目进程.md`。

保留本文件只是为了兼容旧提示词和旧路径引用，避免历史入口失效。

English:
The active DateFac Agent progress source has moved to:

```text
docs/agent/项目进程.md
```

The old milestone ledger path is now a historical archive pointer, not the default preflight document for active 348 Agent tasks.
