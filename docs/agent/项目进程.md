# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
R7AE: file-backed source_text sidecar loader design completed
R7AF: test-only file-backed source_text sidecar loader implemented
```

R7AF 结果：

```text
commit = 3285ca6 test: add source text sidecar loader coverage
Decision = PASS，R7AF test-only sidecar loader implemented
test_result = PASS，pytest tests/agent -q => 162 passed
files_modified = 3
sidecar_loader_result = PASS
fail_closed_result = PASS
evidence_index_validation_result = PASS
review_queue_validation_result = PASS
full_text_serialization_result = PASS
self_qa_result = PASS
readiness_gates = CLOSED
```

R7AF 修改文件：

```text
tests/agent/source_text_sidecar_loader_348n.py
tests/agent/test_source_text_sidecar_loader_348n.py
tests/agent/fixtures/source_text_sidecars/r7af_source_text_sidecar__basic_positive_negative__v1.json
```

R7AF 关键结果：

```text
test-only loader under tests/agent/
JSON object v1 fixture with schema_version=1 and fixture_scope=test_only
exact schema keys, UTF-8 sha256, char_count, positive page_number, trusted_source=true
malformed/unknown/missing/duplicate/hash/count/untrusted invalid cases fail closed
valid fixture drives VERIFIED/DISAGREED/UNVERIFIED through existing source_text wiring
evidence_index and review_queue exclude full source_text
VERIFIED remains WEAK_EVIDENCE and does not alter clean/readiness
```

## 当前任务

```text
348N-R7AF-QA source_text file-backed sidecar loader review
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = QA-review-only
```

任务文档：

```text
docs/codex_tasks/348N_R7AF_QA_source_text_file_backed_sidecar_loader_review.md
```

当前任务类型：QA / review task。

目标：审查 R7AF loader 是否保持 test-only、fail-closed、metadata-only、full source_text 不序列化，并确认没有 production hook、真实 workbook rerun 或 readiness 边界泄漏。

## 当前推荐下一步

```text
执行 348N-R7AF-QA source_text file-backed sidecar loader review
```

R7AF-QA 通过后，建议进入 R7AG single-real-MinerU-artifact adapter design，只做单份真实 MinerU 产物闭环设计，不直接做批量生产平台。
