# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7S: strict-table scaffolding guard
R7V: cross-family clean-boundary validated，readiness gates closed
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
```

R7AB 结果：

```text
commit = 12a4726 feat: wire trusted source text metadata
Decision = PASS，R7AB implementation completed and pushed
test_result = PASS，pytest tests/agent -q => 123 passed
files_modified = 7
source_text_contract_result = PASS
source_text_selection_result = PASS
agreement_injection_result = PASS
evidence_index_wiring_result = PASS
review_queue_wiring_result = ADDED compact fields
readiness_gates = CLOSED
```

R7AB 关键边界：

```text
trusted source_text 只有在 explicit page provenance + source_id/page_number/locator 绑定通过时才传入 checker
无 trusted source_text 时保持默认 UNVERIFIED
不把 Excel raw fields 当 trusted source_text
不默认序列化 full source_text
VERIFIED 不等于 STRONG_EVIDENCE / clean admission / production_ready
```

## 当前任务

```text
348N-R7AB-QA source_text availability / evidence index wiring review
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AB-QA 要确认 trusted source_text selection、checker injection、evidence_index metadata、review_queue compact fields 均未越过 clean/readiness 边界。
```

任务文档：

```text
docs/codex_tasks/348N_R7AB_QA_source_text_availability_evidence_index_wiring_review.md
```

当前任务类型：QA / review task。

目标：审查 R7AB 是否安全实现 source_text metadata 和 checker 注入，确认 default UNVERIFIED、mismatch/untrusted/empty rejection、full text 不序列化、readiness gates 关闭。

## 当前推荐下一步

```text
执行 348N-R7AB-QA source_text availability / evidence index wiring review
```

R7AB-QA 通过后，再考虑是否做真实 workbook rerun 或更窄的 source_text fixture/sidecar integration task。
