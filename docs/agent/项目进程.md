# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
```

R7AD-QA 结果：

```text
commit = 39e01ba docs: add R7AD QA review
Decision = PASS，R7AD-QA confirms fixture dry-run is valid
test_result = PASS，pytest tests/agent -q => 134 passed
files_modified = 1
fixture_dry_run_result = PASS
evidence_index_validation_result = PASS
review_queue_validation_result = PASS
full_text_serialization_result = PASS
qa_result = VALID
readiness_gates = CLOSED
```

R7AD-QA 确认：

```text
R7AD 是 tests-only，使用 in-test SourceTextEvidence
trusted VERIFIED，trusted mismatch DISAGREED
missing / mismatch / untrusted / empty -> UNVERIFIED
evidence_index metadata-only，无 full source_text
review_queue compact fields，无 full source_text
VERIFIED 不影响 STRONG_EVIDENCE / clean admission / readiness
```

## 当前任务

```text
348N-R7AE source_text file-backed sidecar loader design
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AE 要设计 file-backed source_text sidecar loader；loader contract 如果不保守，会把 fixture-only evidence 误扩展成不安全的生产证据。
```

任务文档：

```text
docs/codex_tasks/348N_R7AE_source_text_file_backed_sidecar_loader_design.md
```

当前任务类型：design / review task，不是实现任务。

目标：设计 file-backed source_text sidecar loader 的格式、schema、fixture 位置、fail-closed 规则、hash/text handling、evidence_index/review_queue 验证方式；不运行 workbook rerun，不接 MinerU/OCR/LLM/VLM，不打开 readiness gates。

## 当前推荐下一步

```text
执行 348N-R7AE source_text file-backed sidecar loader design
```

R7AE 通过后，再考虑 R7AF file-backed sidecar loader implementation。不要直接跳到真实 workbook rerun 或 production readiness。
