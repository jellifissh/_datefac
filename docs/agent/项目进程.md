# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
R7AE: file-backed source_text sidecar loader design completed
R7AF: test-only file-backed source_text sidecar loader implemented
R7AF-QA: file-backed source_text sidecar loader QA valid
R7AG: lightweight PDF evidence bridge design valid
R7AH: lightweight PDF evidence bridge test-only prototype implemented
```

R7AH 结果：

```text
commit = 26e2717 test: add lightweight PDF evidence bridge prototype
Decision = PASS; R7AH test-only lightweight bridge prototype implemented
test_result = PASS; pytest tests/agent -q => 180 passed
files_modified = 2
boundary_check = PASS
no_heavy_parser_result = PASS
readiness_gates = CLOSED
```

R7AH 修改文件：

```text
tests/agent/lightweight_pdf_evidence_bridge_348n.py
tests/agent/test_lightweight_pdf_evidence_bridge_348n.py
```

R7AH 关键结果：

```text
test-only helper under tests/agent/
in-memory synthetic page-text provider
target-page lookup + capped candidate search
value + metric + period proximity required for accepted match
value-only / no-value / wrong-page / duplicate ambiguous / missing text / scanned marker fail conservatively
accepted match maps to SourceTextEvidence
evidence_index metadata-only
review_queue compact-only
no full snippet text serialization
no real PDF / PyMuPDF / pdfplumber / pypdf / MinerU / OCR / LLM / VLM / runner / CLI / run_pilot hook
```

## 当前任务

```text
348N-R7AH-QA lightweight PDF evidence bridge prototype review
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = QA-review-only
```

任务文档：

```text
docs/codex_tasks/348N_R7AH_QA_lightweight_pdf_evidence_bridge_prototype_review.md
```

当前任务类型：QA / review task。

目标：审查 R7AH prototype 是否保持 test-only、synthetic-only、no-heavy-parser、metadata-only，并确认没有真实 PDF parser、MinerU/OCR/LLM/VLM、production hook 或 readiness 边界泄漏。

## 当前推荐下一步

```text
执行 348N-R7AH-QA lightweight PDF evidence bridge prototype review
```

R7AH-QA 通过后，建议进入 R7AI real PDF text-layer provider design。不要直接跳到真实批量 PDF、MinerU-first 或 production readiness。
