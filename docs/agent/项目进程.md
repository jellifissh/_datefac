# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
VERIFIED 不等于 STRONG_EVIDENCE
VERIFIED 不等于 clean_data 自动放行
```

## 最新完成

```text
R7AM test-only MinerU artifact adapter prototype completed
commit = 0de3a4a test: add MinerU artifact adapter prototype
pytest tests/agent/test_mineru_artifact_adapter_348n.py -q = 21 passed
pytest tests/agent -q = 201 passed
files_modified = 4
fixture_result = PASS，小型 curated fixture，未提交完整 MinerU output
adapter_result = PASS，test-only content_list_v2 adapter
matching_helper_result = PASS，覆盖 VERIFIED / UNVERIFIED / DISAGREED / AMBIGUOUS / MISSING_EVIDENCE
boundary_check = PASS
readiness_gates = CLOSED
```

R7AM 新增内容：

```text
tests/agent/mineru_artifact_adapter_348n.py
tests/agent/test_mineru_artifact_adapter_348n.py
tests/agent/fixtures/mineru_artifacts/anjing_minimal_content_list_v2__r7am.json
docs/agent/348N_R7AM_TEST_ONLY_MINERU_ARTIFACT_ADAPTER_PROTOTYPE_REPORT.md
```

R7AM 含义：把 R7AL 本地真实对比里证明可行的 MinerU `content_list_v2` -> evidence block 形状，收敛成 `tests/agent/` 下可重复测试的最小原型。它仍不是生产接入，不提交完整 MinerU output，也不打开 readiness gates。

## 当前任务

```text
348N-R7AM-QA test-only MinerU artifact adapter prototype review
```

任务文档：

```text
docs/codex_tasks/348N_R7AM_QA_test_only_mineru_artifact_adapter_prototype_review.md
```

任务类型：QA-review-only。

目标：审查 R7AM 是否真的保持 test-only，fixture 是否小型、adapter 是否保守、matching helper 是否没有误把 value-only / metric-only 判为 VERIFIED，是否没有修改生产代码、没有提交完整产物、没有打开 readiness gates。
