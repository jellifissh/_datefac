# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线不是重新抽 PDF，也不是训练模型，而是对已有 Excel 抽取结果做确定性审计：识别 row type、检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

当前阶段仍是严格审计 / demo-only / review-first。任何任务都不得把当前结果描述为生产可用或正式客户可交付。

## 已验证 workbook families

### Anjing / 安井

```text
PDF: H3_AP202606081823352906_1_331fresh_20260615_21591.pdf
Excel: 安井食品研报数据汇总.xlsx
R7U clean_data_row_count = 65
R7U review_queue_row_count = 17
market_reference_row_count = 10
R7S removed normal fact rows = 0
```

安井当前表现：正常财务事实保留，market reference 行已移出 clean_data。R7S strict-table scaffolding guard 没有误伤安井的正常数字 fact 行。

### Linyang / 林洋

```text
PDF: 6862e6f3995d3dbfbed310b51601fb0a.pdf
Excel: linyang_energy_pdf_extracted_testset (1).xlsx
R7U clean_data_row_count = 0
R7U review_queue_row_count = 489
unknown_row_count = 0
market_reference_row_count = 10
```

林洋当前表现：testset / supporting / market reference 结构识别稳定，clean_data 保持 0，R7S 未造成回归。

### Taihao / 泰豪

```text
PDF: H3_AP202605231822706325_1.pdf
Excel: 泰豪科技_深度研报_核心数据提取_豆包AI生成 (1).xlsx
R7Q clean_data_row_count = 92
R7Q review_queue_row_count = 66
R7T clean_data_row_count = 72
R7T review_queue_row_count = 86
unknown_row_count = 0
market_reference_row_count = 2
```

泰豪当前表现：R7S 后 20 个 strict-table scaffolding / pseudo-header / comparison rows 从 clean_data 移入 review_queue；`市场数据 / 厂商 / 对比维度 / 订单日期 / 项目 / 指标` 等风险行不再留在 clean_data。

## 348N Agent foundation 历史摘要

### R1-R3：林洋 unknown family refinement

```text
R1: 诊断 unknown 来源，确认 normalized_testset 是长表 testset schema
R2: normalized_testset -> NORMALIZED_TESTSET_RECORD_ROW，保持 review-only
R3: README / data_dictionary / doc_metadata / figure_index / related_research / validation_checks -> TESTSET_SUPPORTING_ROW；market_base_data -> MARKET_REFERENCE_ROW
R3-QA: unknown_row_count = 0，clean_data 未被 supporting/testset 污染
```

### R4-R6：qualitative_facts 与 output guardrails

```text
R5: 修复 qualitative_facts 中文表头识别，clean_data_row_count 33 -> 0
R6B: 引入 stdlib-only output_schema_guardrails
R6B-FIX: clean_data_csv_row_count / review_queue_csv_row_count 物理计数纳入 guardrail 契约
R6C: 建议把 output guardrails 作为 runner contract
```

### R7P-R7Q：Taihao market reference 边界

```text
R7P: Taihao pilot 暴露 MARKET_REFERENCE_ROW clean_data 泄漏，典型行 = 收盘价
R7P-FIX: root cause = MARKET_REFERENCE_ROW + WEAK_EVIDENCE 被允许进入 INTERNAL_REFERENCE_CANDIDATE
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7P-FIX2-QA: market reference clean_data 泄漏修复有效
R7Q: post-FIX2 Taihao review 暴露新的 strict-table pseudo-header / comparison-row clean-boundary 风险
```

R7Q 基线：

```text
clean_data_row_count = 92
clean_data_csv_row_count = 92
review_queue_row_count = 66
review_queue_csv_row_count = 66
unknown_row_count = 0
market_reference_row_count = 2
```

### R7R-R7V：strict-table scaffolding clean-boundary 收束

```text
R7R: 设计结论 = 不新增 row_type，在 clean_candidate_policy 层收窄 STRICT_FINANCIAL_TABLE_ROW + WEAK_EVIDENCE admission
R7S: 实现 scaffolding guard；tests/agent = 86 passed
R7S-QA: 确认 guard 有效，未改变 MARKET_REFERENCE_ROW / qualitative_facts / readiness gates
R7T: Taihao rerun，clean 92 -> 72，review 66 -> 86，20 个风险行移入 review_queue
R7U: Linyang / Anjing 回归验证通过，无 R7S 回归
R7V: 跨 family readiness review 完成，clean-boundary validated，但 readiness gates 继续关闭
```

R7V 结论：

```text
348N_R7V_CONFIRMED_CROSS_FAMILY_CLEAN_BOUNDARY_VALID_READINESS_GATES_REMAIN_CLOSED
production_ready = no
formal_client_export_allowed = no
```

### R7W-R7X：evidence strengthening 起步

```text
R7W: 设计 WEAK_EVIDENCE -> STRONG_EVIDENCE 路径
R7X: 实现 page_number parsing + agreement_status 字段
```

R7W 关键原则：

```text
Strong evidence must mean traceable and auditable, not merely plausible.
```

R7X 当前实现：

```text
modified files = 5
commit = 5d8aa24 feat: add evidence provenance parsing
page_number parsing = implemented
agreement_status = MISSING / UNVERIFIED / VERIFIED / DISAGREED
R7X only populates MISSING / UNVERIFIED
explicit/page provenance + UNVERIFIED -> remains WEAK_EVIDENCE
tests/agent = 95 passed
full tests = 33 historical legacy failures, unrelated to R7X
```

R7X 的关键边界：解析出页码只表示 provenance anchor 存在，不表示 source-value agreement 已核验；`UNVERIFIED` 不得自动升级为 verified `STRONG_EVIDENCE`。

## 当前任务

```text
348N-R7X-QA evidence provenance parsing review
```

任务文档：

```text
docs/codex_tasks/348N_R7X_QA_evidence_provenance_parsing_review.md
```

当前任务类型：QA / review task。

目标：审查 R7X 实现是否正确保持以下边界：

```text
page_number parsed = provenance anchor exists
agreement_status = UNVERIFIED = source-value agreement not checked yet
STRONG_EVIDENCE must not be claimed solely from parsed page_number
```

## 当前推荐下一步

```text
先执行 348N-R7X-QA
如果 QA 通过，再设计下一步 R7Y：deterministic source-value agreement checker / evidence agreement verification slice
```

R7Y 不应直接追求 production readiness，而应继续补足强证据链：从 page provenance 进入 deterministic value agreement verification。
