# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
VERIFIED 不等于 STRONG_EVIDENCE
VERIFIED 不等于 clean_data 自动放行
```

## 最新完成

```text
R7AN test-only MinerU adapter controlled comparison dry-run design completed
commit = f29cc07 docs: add R7AN controlled comparison design
Decision = PASS
pytest tests/agent/test_mineru_artifact_adapter_348n.py -q = 21 passed
pytest tests/agent -q = 201 passed
input_scope_decision = full Anjing DateFac Excel + full local MinerU content_list_v2 for R7AO, optional smoke mode only
runner_scope_decision = local test-only runner under output/comparison/anjing_foods_mineru_adapter_r7ao/
adapter_usage_decision = reuse tests.agent.mineru_artifact_adapter_348n directly
output_policy_decision = write local xlsx/csv/md/json reports, do not commit outputs
boundary_check = PASS
readiness_gates = CLOSED
```

R7AN 结论：下一步不是接生产，而是在本地 output 目录写一个 no-commit controlled comparison runner，用 R7AM adapter 形状跑完整安井样本对比，输出本地报告。

## 当前任务

```text
348N-R7AO test-only MinerU adapter controlled comparison runner
```

任务文档：

```text
docs/codex_tasks/348N_R7AO_test_only_mineru_adapter_controlled_comparison_runner.md
```

任务类型：single-run-data-analysis + no-commit。

目标：用完整安井 DateFac Excel + 完整本地 MinerU `content_list_v2`，复用 R7AM test-only adapter，执行 controlled comparison runner。只在本地 `output/comparison/anjing_foods_mineru_adapter_r7ao/` 写 runner 和报告，不 commit、不 push、不改 production code。
