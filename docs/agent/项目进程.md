# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
VERIFIED 不等于 STRONG_EVIDENCE
VERIFIED 不等于 clean_data 自动放行
```

## 最新完成

```text
R7BN-QA review_queue persistence contract handoff checkpoint review completed
commit = b223c05 docs: add R7BN QA review
Decision = PASS，R7BN handoff checkpoint is accurate and no-overclaim
py_compile = PASS
persistence targeted tests = 76 passed
schema alignment tests = 29 passed
dry-run integration tests = 36 passed
writer contract tests = 24 passed
adapter skeleton tests = 75 passed
full tests/agent = 480 passed
boundary_check = PASS
readiness_gates = CLOSED
```

R7BN-QA 结论：R7BN handoff checkpoint 准确总结了 R7BL/R7BM test-only persistence contract 里程碑。当前只证明了测试区内存候选批次边界，不代表真实落库、生产持久化、正式导出或 readiness 打开。

## 当前 review_queue 测试区安全链路

```text
adapter candidate output
-> test-only dry-run integration boundary
-> test-only writer dry-run preview
-> test-only schema alignment contract
-> test-only persistence contract
-> in-memory persistence candidate batch
```

这条链路证明：

```text
字段分类可审计
future preview metadata-first
required fields fail closed
forbidden fields fail closed
idempotency key deterministic
record_payload_hash deterministic
batch fail-closed / no partial candidate batch
bounded evidence_preview allowed
full source_text forbidden
raw MinerU / raw Excel / raw parser / raw LLM-VLM payloads forbidden
clean_data intent / delivery export intent / production writer config forbidden
```

## 里程碑记录

### R7BL

```text
R7BL review_queue persistence contract test-only prototype completed
commit = c76499e test: add review queue persistence contract prototype
targeted persistence tests = 37 passed
```

R7BL 创建 test-only persistence contract prototype，只接受 validated schema alignment preview，输出 in-memory persistence candidate batch，不写数据库、不写文件、不导出、不接生产。

### R7BL-QA

```text
R7BL-QA review completed
commit = 7713db4 docs: add R7BL QA review
Decision = PASS
```

R7BL-QA 确认 prototype disabled-by-default、explicit-token-gated、metadata-first、source_text-safe、clean_data-safe、delivery/export-safe、no-hook、no-IO、readiness-closed。

### R7BM

```text
R7BM negative-path expansion completed
commit = 9daca34 test: expand review queue persistence negative paths
targeted persistence tests = 76 passed
```

R7BM 扩展负路径：nested forbidden fields、hidden clean/export/production/readiness intent、mixed valid+invalid batch、user-supplied record_payload_hash、duplicate idempotency_key/review_item_id、bad hash/source shape、bad evidence_preview、bad reviewer action/status 全部 fail closed。

### R7BM-QA

```text
R7BM-QA review completed
commit = ce10b99 docs: add R7BM QA review
Decision = PASS
full tests/agent = 480 passed
```

R7BM-QA 确认负路径覆盖保守且仍然 test-only，没有生产 hook、没有 DB、没有 output、没有 dependency/readiness 变更。

### R7BN

```text
R7BN handoff checkpoint completed
commit = 4162ea6 docs: add R7BN persistence handoff checkpoint
```

R7BN 把当前 persistence contract 阶段、链路、验证、边界、剩余风险和下一步写成交接检查点。

### R7BN-QA

```text
R7BN-QA checkpoint review completed
commit = b223c05 docs: add R7BN QA review
Decision = PASS
```

R7BN-QA 确认 checkpoint 准确、完整、保守，没有把 in-memory candidate batch 说成真实落库或生产 ready。

### R7BO

```text
R7BO project documentation sync after review_queue persistence contract milestone
```

R7BO 将 R7BL/R7BM/R7BN-QA 后的状态同步到项目主文档。它仍然是 docs-sync-only，不修改代码、测试、fixture、output、dependency、生产集成或 readiness gates。

## 仍未完成

```text
真实 review_queue persistence 尚未实现
DB model / repository / migration 尚未实现
storage code 尚未实现
production writer 尚未实现
runner / CLI / production pipeline hook 尚未接入
transaction / rollback 行为只在内存中模拟
storage performance / concurrency 尚未证明
真实 review UI integration 尚未证明
clean_data 自动写入仍然关闭
formal delivery/export 仍然关闭
client/export readiness 不成立
```

## 当前任务

```text
348N-R7BO project documentation sync after review_queue persistence contract milestone
```

任务文档：

```text
docs/codex_tasks/348N_R7BO_project_documentation_sync_after_review_queue_persistence_contract_milestone.md
```

任务类型：docs-sync-only。

目标：把 R7BL/R7BM/R7BN-QA 完成的 review_queue persistence contract test-only 里程碑同步到项目主文档，并明确下一步应先做 R7BO-QA 文档同步审查。
