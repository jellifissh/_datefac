# DateFac Agent 项目进程

> 当前主线：`DateFac Agent`，围绕金融文档 AI 抽取结果做审计、复核队列、证据约束和内部 clean-data candidate 输出。
>
> 当前工作区：`D:\_datefac_agent`
>
> 当前分支：`pivot/348-agent-foundation`
>
> 当前流程：`intake -> audit -> review -> delivery`

---

## 0. 当前工程约束

```text
不跑 MinerU
不调用 LLM/VLM
不 OCR
不重新抽 PDF
不覆盖历史 output
不触碰旧 D:\_datefac 工作区
不触碰 legacy datefac/ 主包
不提交 output 文件
不声明 client_ready
不声明 production_ready
不打开 formal_client_export_allowed
```

---

## 1. 第一真实 workbook：348A 系列

第一样本：

```text
PDF:   H3_AP202606081823352906_1_331fresh_20260615_21591.pdf
Excel: 安井食品研报数据汇总.xlsx
```

关键结果：

```text
348A 初始链路跑通，但 evidence policy 和 row-type policy 过粗
348A-R1 拆分 WEAK_EVIDENCE 与 MISSING_EVIDENCE
348A-R2 新增 STRICT_FINANCIAL_TABLE_ROW / MARKET_REFERENCE_ROW / NARRATIVE_ASSERTION / UNKNOWN_ROW
348A-R3 修复 净资产收益率(%) monetary_unit_mismatch 误报
348A-R4 形成 internal clean-data candidate policy
348A-R4-QA 确认 clean_data 无 narrative、period issue、unit/valuation issue
```

第一样本稳定结果：

```text
clean_data_row_count = 75
review_queue_row_count = 7
client_ready = false
production_ready = false
formal_client_export_allowed = false
```

---

## 2. 第二真实 workbook：348S / R1 / R2

第二样本：

```text
PDF:   H3_AP202605231822706325_1.pdf
Excel: H3_AP202605231822706325_1_提取结果.xlsx
```

348S 初始：

```text
row_count_total = 119
unit_issue_count = 2
period_issue_count = 66
unknown_row_count = 53
clean_data_row_count = 0
review_queue_row_count = 119
```

348S-R1 后：

```text
row_count_total = 112
unknown_row_count = 0
period_issue_count = 5
clean_data_row_count = 87
review_queue_row_count = 25
```

348S-R2 后：

```text
row_count_total = 112
fail_count = 0
unit_issue_count = 0
period_issue_count = 0
clean_data_row_count = 94
review_queue_row_count = 18
unknown_row_count = 0
```

348S-R2-QA 结论：

```text
348S_R2_QA_CONFIRMED_RESIDUAL_REFINEMENT_VALID
```

---

## 3. Fixture 阶段：348F / 348F-QA

新增 fixture-backed tests：

```text
test_unit_semantic_fixture_cases
test_period_detection_fixture_cases
test_clean_candidate_routing_fixture_cases
```

QA 结论：

```text
348F_QA_CONFIRMED_FIXTURE_HARVEST_VALID
```

---

## 4. 第三真实 workbook：348S Third / R3 / R3B / QA

第三样本配对：

```text
PDF:   H3_AP202605231822706325_1.pdf
Excel: 泰豪科技_深度研报_核心数据提取_豆包AI生成 (1).xlsx
```

### 348S Third Workbook Pilot

首次 runner 在 intake 阶段失败：

```text
failure type = IndexError: list index out of range
failure location = datefac_agent/intake/excel_intake.py
failure trigger = _find_key_value_start() assumed at least two text cells in a row
```

结论：

```text
348S_THIRD_WORKBOOK_PILOT_CONFIRMED_NEEDS_SCHEMA_REFINEMENT
```

### 348S-R3

R3 已修复短行 crash，但 runner 产出为空：

```text
pytest passed: 30 passed
sheet_count = 8
row_count_total = 0
clean_data_row_count = 0
review_queue_row_count = 0
```

结论：

```text
348S_R3_CONFIRMED_PARTIAL_FIX_STILL_BLOCKED
```

### 348S-R3B

R3B 根因：第三 workbook 的 worksheet XML 有真实表格，但 cached dimension 错写为 `A1/A1:A1`，`openpyxl read_only=True` 只暴露每个 sheet 的第一行。

R3B 窄修：检测 `A1:A1` cached dimension 后调用 `reset_dimensions()`，不改 audit policy。

R3B 结果：

```text
pytest passed: 32 passed
third workbook runner completed
row_count_total = 158
clean_data_row_count = 94
review_queue_row_count = 64
unknown_row_count = 44
```

R3B 结论：

```text
348S_R3B_CONFIRMED_ZERO_ROW_INTAKE_FIXED
```

### 348S-QA Third Workbook Pilot Review

QA 结论：

```text
348S_QA_THIRD_WORKBOOK_REVIEW_CONFIRMED_NEEDS_R3C_UNKNOWN_ROW_REFINEMENT
```

QA 确认：

```text
clean_data_row_count = 94 干净，仅含 weak_evidence，无 blocking issues
review_queue_row_count = 64 可解释，没有 clean candidates 污染
unknown_row_count = 44 偏高，是最强 refinement signal
percentage_unit_missing = 10 基本是真 review signal，集中在 STRICT_FINANCIAL_TABLE_ROW
period_values_missing = 8 全部集中在 STRICT_FINANCIAL_TABLE_ROW
monetary_unit_mismatch = 1 与 valuation_metric_unit_suspicious = 1 落在同一条 mixed narrative+valuation UNKNOWN_ROW
weak_evidence_count = 158，missing_evidence_count = 0，符合当前阶段
pytest passed: 32 passed
```

推荐下一步：

```text
348S-R3C Unknown Row Refinement
```

---

## 5. 当前任务

当前任务：

```text
348S-R3C Unknown Row Refinement
```

任务文档：

```text
docs/codex_tasks/348S_R3C_unknown_row_refinement.md
```

固定交接入口：

```text
docs/project_handoffs/CURRENT_MODEL_HANDOFF.md
```

当前任务性质：窄范围 row-type / routing refinement。

重点：

```text
只处理 unknown_row_count = 44
不改 unit / period / valuation checker
不改 evidence policy
不改 clean candidate policy
不提交 output
不跑 MinerU / OCR / LLM
```

---

## 6. 当前阶段总结

```text
第一样本：clean_data_row_count = 75，review_queue_row_count = 7
第二样本 R2：clean_data_row_count = 94，review_queue_row_count = 18
第三样本 R3B：row_count_total = 158，clean_data_row_count = 94，review_queue_row_count = 64
第三样本 QA：clean_data 干净，但 unknown_row_count = 44 偏高
fixture-backed tests: 32 passed
```

当前仍保持：

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
LLM / MinerU / OCR = 0
```

下一步：

```text
348S-R3C Unknown Row Refinement
```
