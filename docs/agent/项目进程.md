# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
VERIFIED 不等于 STRONG_EVIDENCE
VERIFIED 不等于 clean_data 自动放行
```

## 最新完成

```text
R7AO-QA test-only MinerU adapter controlled comparison runner review completed
commit = 6e02622 docs: add R7AO QA review
Decision = PASS，R7AO local runner/output QA valid
py_compile = PASS
pytest = 21 passed + 201 passed
files_modified = 1
runner_review_result = PASS，local-only, reuses tests.agent.mineru_artifact_adapter_348n, no production hook
metadata_review_result = PASS，counts/inputs/branch/head/readiness recorded
comparison_output_review_result = PASS，xlsx/md/json/csv complete, metadata-only evidence CSV
count_sanity_result = PASS，451 = 402 VERIFIED + 49 non-VERIFIED
probe_examples_review_result = PASS，11/11 VERIFIED
boundary_check = PASS
readiness_gates = CLOSED
```

R7AO-QA 结论：R7AO 本地 runner 和输出可信，且没有 tracked 文件污染。下一步不直接接生产，先设计差异复核工作流。

## 当前任务

```text
348N-R7AP DateFac-MinerU discrepancy review workflow design
```

任务文档：

```text
docs/codex_tasks/348N_R7AP_DateFac_MinerU_discrepancy_review_workflow_design.md
```

任务类型：design-review-only。

目标：设计 49 条 non-VERIFIED rows 如何进入可审计的 discrepancy review workflow，明确 DISAGREED / AMBIGUOUS / MISSING_EVIDENCE / PARSE_SKIPPED 的复核队列策略、schema、reviewer action、clean_data 后置策略和导出策略。
