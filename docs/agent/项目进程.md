# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
R7AE: file-backed source_text sidecar loader design completed
R7AF: test-only file-backed source_text sidecar loader implemented
R7AF-QA: file-backed source_text sidecar loader QA valid
```

R7AF-QA 结果：

```text
commit = 2723251 docs: add R7AF QA review
Decision = 348N_R7AF_QA_CONFIRMED_SOURCE_TEXT_FILE_BACKED_SIDECAR_LOADER_VALID
qa_result = VALID
test_result = PASS，pytest tests/agent -q => 162 passed
LLM / MinerU / OCR calls = 0
readiness_gates = CLOSED
```

R7AF-QA 确认：

```text
loader remains test-only under tests/agent/
no datefac_agent / runner / CLI / run_pilot / production hook
JSON object v1 strict schema
malformed/unknown/missing/duplicate/bad hash/bad char_count/untrusted all fail closed
no partial records
evidence_index metadata-only
review_queue compact fields only
full source_text not serialized
VERIFIED does not promote to STRONG_EVIDENCE, clean_data, or readiness
```

## 当前任务

```text
348N-R7AG lightweight PDF evidence bridge design
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = design-review-only
```

任务文档：

```text
docs/codex_tasks/348N_R7AG_lightweight_pdf_evidence_bridge_design.md
```

当前任务类型：design / review task。

目标：把下一步从 MinerU-first 改成 lightweight PDF evidence bridge。设计如何用 PDF 文本层、页码、数字锚点、关键词/期间邻近关系，把现有产物和原 PDF 建立低成本证据联系；MinerU 只作为 fallback，不作为默认入口。

## 当前推荐下一步

```text
执行 348N-R7AG lightweight PDF evidence bridge design
```

R7AG 通过后，建议进入 R7AH lightweight PDF evidence bridge test-only prototype。不要直接做批量 PDF 平台，也不要默认接 MinerU。
