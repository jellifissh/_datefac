# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
D:\_datefac_agent
pivot/348-agent-foundation
intake -> audit -> review -> delivery
```

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
LLM / MinerU / OCR = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 第一真实 workbook

```text
PDF: H3_AP202606081823352906_1_331fresh_20260615_21591.pdf
Excel: 安井食品研报数据汇总.xlsx
clean_data_row_count = 75
review_queue_row_count = 7
```

已完成 evidence、row type、unit false positive、clean-data candidate policy 和 QA。

## 第二真实 workbook

```text
PDF: H3_AP202605231822706325_1.pdf
Excel: H3_AP202605231822706325_1_提取结果.xlsx
```

最终 R2 结果：

```text
row_count_total = 112
unit_issue_count = 0
period_issue_count = 0
unknown_row_count = 0
clean_data_row_count = 94
review_queue_row_count = 18
```

QA 结论：

```text
348S_R2_QA_CONFIRMED_RESIDUAL_REFINEMENT_VALID
```

## Fixture 阶段

已完成 348F fixture harvest 和 QA。

```text
348F_QA_CONFIRMED_FIXTURE_HARVEST_VALID
```

## 第三真实 workbook

第三样本：

```text
PDF: H3_AP202605231822706325_1.pdf
Excel: 泰豪科技_深度研报_核心数据提取_豆包AI生成 (1).xlsx
```

### R3

修复短行 crash，但结果仍为空。

```text
348S_R3_CONFIRMED_PARTIAL_FIX_STILL_BLOCKED
row_count_total = 0
```

### R3B

修复 workbook cached dimension 导致 read-only 只读第一行的问题。

```text
348S_R3B_CONFIRMED_ZERO_ROW_INTAKE_FIXED
row_count_total = 158
clean_data_row_count = 94
review_queue_row_count = 64
unknown_row_count = 44
```

### Third workbook QA

QA 确认 clean data 干净，review queue 可解释，但 unknown rows 偏高。

```text
348S_QA_THIRD_WORKBOOK_REVIEW_CONFIRMED_NEEDS_R3C_UNKNOWN_ROW_REFINEMENT
```

### R3C

R3C 对第三 workbook 的 unknown rows 做窄范围 row-type refinement。

```text
unknown_row_count: 44 -> 0
narrative_assertion_count: 2 -> 46
clean_data_row_count: 94 -> 94
review_queue_row_count: 64 -> 64
unit_issue_count: 11 -> 11
period_issue_count: 8 -> 8
valuation_issue_count: 1 -> 1
pytest: 36 passed
```

R3C 结论：

```text
348S_R3C_CONFIRMED_UNKNOWN_ROW_REFINEMENT_VALID
```

## 当前主线（348N Agent foundation）

```text
348N Agent foundation pivot
工作区：D:\_datefac_agent / pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
样本：linyang_energy_pdf_extracted_testset
```

### 348N-R1 / R2 / R3 摘要

```text
348N-R2 normalized_testset schema support
348N_R2_QA_CONFIRMED_NORMALIZED_TESTSET_SCHEMA_SUPPORT_VALID
normalized_testset_record_row_count = 320

348N-R3 remaining non-normalized unknown-family refinement
348N_R3_CONFIRMED_REMAINING_UNKNOWN_FAMILY_REFINEMENT_VALID
unknown_row_count: 48 -> 0
clean_data_row_count: 37 -> 33
review_queue_row_count: 447 -> 455
testset_supporting_row_count: 0 -> 49
market_reference_row_count: 2 -> 10
pytest: 48 passed

348N-R3-QA review
348N_R3_QA_CONFIRMED_REMAINING_UNKNOWN_FAMILY_REFINEMENT_VALID
```

## 当前任务

```text
348N-DOCS-CLEANUP AI Handoff Documentation Simplification
```

任务文档：

```text
docs/codex_tasks/348N_DOCS_CLEANUP_ai_handoff_simplification.md
```

交接入口：

```text
docs/project_handoffs/CURRENT_MODEL_HANDOFF.md
```

当前任务是 docs-only cleanup，不是代码修复。

重点：

```text
减少 AGENTS / handoff / skills / codex_tasks 之间的重复说明
明确文档职责：AGENTS=稳定规则，handoff=当前阶段，skills=可复用工作流，codex_tasks=完整任务书，agent 报告=证据
保留历史 result / QA 报告，不重写项目历史
```

下一步候选：

```text
348N-R4 clean data candidate policy review
```
