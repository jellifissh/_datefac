# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线不是重新抽 PDF，也不是训练模型，而是对已有 Excel 抽取结果做确定性审计：识别 row type、检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

当前阶段仍是严格审计 / demo-only / review-first。任何任务都不得把当前结果描述为生产可用或正式客户可交付。

## 已验证 workbook families

```text
Anjing / 安井：R7U clean_data=65, review_queue=17, market_reference=10, R7S removed normal facts=0
Linyang / 林洋：R7U clean_data=0, review_queue=489, unknown=0, market_reference=10
Taihao / 泰豪：R7T clean_data 92 -> 72, review_queue 66 -> 86, 20 risky scaffolding rows moved to review_queue
```

## 历史摘要

### R1-R6：林洋 schema / qualitative_facts / guardrails

```text
R1-R3: normalized_testset / supporting / market reference sheet families 被显式识别，unknown_row_count 收敛到 0
R5: qualitative_facts 中文表头识别修复，clean_data_row_count 33 -> 0
R6B/R6B-FIX: output_schema_guardrails + logical/physical CSV count guardrail 成为 runner contract
```

### R7P-R7V：clean-boundary 收束

```text
R7P: Taihao pilot 暴露 MARKET_REFERENCE_ROW clean_data 泄漏
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7Q: 暴露 STRICT_FINANCIAL_TABLE_ROW pseudo-header / comparison-row clean-boundary 风险
R7R: 设计结论 = 不新增 row_type，在 clean_candidate_policy 层收窄 admission
R7S: 实现 scaffolding guard；tests/agent = 86 passed
R7T: Taihao rerun，clean 92 -> 72，review 66 -> 86，20 个风险行移入 review_queue
R7U: Linyang / Anjing 回归验证通过，无 R7S 回归
R7V: 跨 family readiness review 完成，clean-boundary validated，但 readiness gates 继续关闭
```

### R7W-R7Y：evidence strengthening

```text
R7W: 设计 WEAK_EVIDENCE -> STRONG_EVIDENCE 路径
R7X: 实现 page_number parsing + agreement_status 字段
R7X-QA: 确认 page_number parsed 不等于 VERIFIED；UNVERIFIED 不等于 STRONG_EVIDENCE
R7Y: 实现 deterministic source-value agreement checker
```

R7W 关键原则：

```text
Strong evidence must mean traceable and auditable, not merely plausible.
```

R7X-QA 结果：

```text
commit = 6fb00ec docs: add R7X QA review
Decision = 348N_R7X_QA_CONFIRMED_EVIDENCE_PROVENANCE_PARSING_VALID
test_result = tests/agent 95 passed
qa_result = VALID
readiness_gates = closed
```

R7Y 结果：

```text
commit = 973ff68 feat: add deterministic evidence agreement checker
Decision = PASS，R7Y deterministic source-value agreement checker 已完成并推送
build_result = PASS，py_compile 全部通过
test_result = PASS，pytest tests/agent -q => 106 passed
files_modified = 2
agreement_checker_result = PASS
verified_status_result = PASS
disagreed_status_result = PASS
strong_evidence_claim_result = PASS，VERIFIED 未映射为 STRONG_EVIDENCE
readiness_gates = CLOSED
```

R7Y 的关键边界：

```text
VERIFIED 只在所有 numeric period_values 被 source_text 数字 token 确定性覆盖时产生
DISAGREED 只在 source text 有数字且完全无匹配时产生
partial coverage -> UNVERIFIED
text-only facts -> UNVERIFIED
no source_text -> UNVERIFIED
VERIFIED != STRONG_EVIDENCE
VERIFIED != clean admission
VERIFIED != production_ready
```

## 当前任务

```text
348N-R7Y-QA deterministic source-value agreement checker review
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7Y-QA 审查 VERIFIED / DISAGREED 证据一致性语义，错误放行会污染后续 evidence_strength 和 readiness 判断。
```

任务文档：

```text
docs/codex_tasks/348N_R7Y_QA_deterministic_source_value_agreement_checker_review.md
```

当前任务类型：QA / review task。

目标：审查 R7Y 是否安全引入 deterministic source-value agreement checker，尤其确认 `VERIFIED` / `DISAGREED` 的产生条件、numeric normalization、partial coverage、text-valued facts、MARKET_REFERENCE_ROW、qualitative_facts、R7S clean-boundary、readiness gates 均未越界。

## 当前推荐下一步

```text
执行 348N-R7Y-QA deterministic source-value agreement checker review
```

R7Y-QA 通过后，再考虑下一步是否做 R7Z：把 source_text 输入接入真实 pipeline / evidence index，或先做 targeted fixture coverage。不要从 R7Y 直接跳到 production readiness。
