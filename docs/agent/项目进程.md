# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
```

## 最新完成

```text
R7AJ dependency audit completed
commit = 6ceddc0 docs: add R7AJ dependency audit
Decision = PASS，348N_R7AJ_DEPENDENCY_AUDIT_CONFIRMED_NO_DECLARED_PDF_TEXT_LAYER_DEPENDENCY
pdf_library_available_result = NO_DECLARED_PROJECT_PDF_TEXT_LAYER_LIBRARY
implementation_readiness_result = NOT_READY
recommended_next_task = 348N-R7AK optional PDF dependency addition design
readiness_gates = CLOSED
```

R7AJ 结论：当前项目只声明了最小 `requirements.txt`，没有声明 PyMuPDF/fitz、pdfplumber、pypdf/PyPDF2、pdfminer.six、reportlab 等 PDF 文本层依赖。本地环境包不算项目依赖，所以不能直接实现真实 PDF provider。

## 当前任务

```text
348N-R7AK optional PDF dependency addition design
```

任务文档：

```text
docs/codex_tasks/348N_R7AK_optional_pdf_dependency_addition_design.md
```

任务类型：design / review task。

目标：只设计 optional PDF dependency addition，不新增依赖、不安装包、不实现 PDF parser、不跑真实 PDF。重点选择第一候选库、optional import、missing dependency status、QA gates，并继续保持 MinerU/OCR fallback-only。
