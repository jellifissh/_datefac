# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
R7AE: file-backed source_text sidecar loader design completed
R7AF: test-only file-backed source_text sidecar loader implemented
R7AF-QA: file-backed source_text sidecar loader QA valid
R7AG: lightweight PDF evidence bridge design valid
```

R7AG 结果：

```text
commit = ea68e33 docs: add R7AG lightweight evidence bridge design
Decision = PASS; 348N_R7AG_LIGHTWEIGHT_PDF_EVIDENCE_BRIDGE_DESIGN_VALID
test_result = PASS; pytest tests/agent -q => 162 passed
files_modified = 1
mineru_default_policy = NOT_DEFAULT
lightweight_bridge_design_result = PASS
cost_control_result = PASS
readiness_gates = CLOSED
```

R7AG 设计结论：

```text
MinerU-first rejected as default
existing extraction products use page_number / metric_name / period / value as hints
page-number rows extract target pages only
no-page rows use capped candidate search
trusted snippets require value + metric + period proximity
raw Excel/JSON excerpts remain hints only
full source_text not serialized
VERIFIED remains non-promotional
MinerU/OCR/manual review are fallback paths only
```

## 当前任务

```text
348N-R7AH lightweight PDF evidence bridge test-only prototype
```

任务大小与执行方式：

```text
task_size = large
recommended_reasoning_level = max
execution_mode = phased-hard-stop + implementation-with-self-QA
```

任务文档：

```text
docs/codex_tasks/348N_R7AH_lightweight_pdf_evidence_bridge_test_only_prototype.md
```

当前任务类型：test-only implementation + tests + self-QA。

目标：用 synthetic page-text fixtures 实现轻量 evidence bridge 原型，验证 page_number / metric / period / value 锚点能生成 SourceTextEvidence，并继续验证 evidence_index/review_queue 不序列化 full source_text。不跑真实 PDF，不新增依赖，不接 MinerU/OCR/LLM/VLM，不接 production path。

## 当前推荐下一步

```text
执行 348N-R7AH lightweight PDF evidence bridge test-only prototype
```

R7AH 通过后，建议进入 R7AH-QA。不要直接接真实 PDF、批量 PDF 平台、MinerU-first 或 production readiness。
