# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 已验证 workbook families

```text
Anjing / 安井：R7U clean_data=65, review_queue=17, market_reference=10
Linyang / 林洋：R7U clean_data=0, review_queue=489, unknown=0, market_reference=10
Taihao / 泰豪：R7T clean_data 92 -> 72, review_queue 66 -> 86
```

## 近期阶段摘要

```text
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7S: strict-table scaffolding guard
R7V: 跨 family clean-boundary validated，readiness gates 继续关闭
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: agreement checker 改为 multiplicity-aware matching
R7AA: 完成 source_text integration / evidence index wiring 设计
```

R7AA 结果：

```text
commit = e5ec327 docs: add R7AA source text integration design
Decision = PASS，R7AA source_text integration design completed
test_result = PASS，pytest tests/agent -q => 111 passed
source_text_availability_result = CURRENT_PIPELINE_HAS_NO_TRUSTED_SOURCE_TEXT
integration_design_result = PASS
evidence_index_design_result = PASS
readiness_gates = CLOSED
```

R7AA 设计结论：

```text
现有 active pipeline 没有 trusted production source_text carrier
Excel 字段只能作为 provenance hints / workbook-side extracted fields
source_text 应通过 provenance-tied sidecar/index 接入
缺失、不可信、source/page 不匹配时保持 UNVERIFIED
不默认在 evidence_index / review_queue 存全文
```

## 当前任务

```text
348N-R7AB source_text availability / evidence index wiring implementation
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AB 是 source_text metadata 和 checker 注入的第一段实现，必须保持默认 UNVERIFIED，不得改变 clean/readiness 边界。
```

任务文档：

```text
docs/codex_tasks/348N_R7AB_source_text_availability_evidence_index_wiring_implementation.md
```

当前任务类型：implementation + tests。

目标：实现最小 source_text sidecar/index contract、可信 source_text 选择、checker-call-time 注入、evidence_index metadata，并在必要时增加 review_queue compact fields；不接 OCR/PDF/LLM/VLM，不打开 readiness gates。

## 当前推荐下一步

```text
执行 348N-R7AB source_text availability / evidence index wiring implementation
```

R7AB 完成后应先做 R7AB-QA，再考虑真实 workbook rerun 或更宽的 source_text pipeline。
