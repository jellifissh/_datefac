# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7S: strict-table scaffolding guard
R7V: cross-family clean-boundary validated，readiness gates closed
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
```

R7AC 结果：

```text
commit = a3cc7f8 docs: add R7AC source text fixture design
Decision = PASS，R7AC design completed
build_result = PASS
test_result = PASS，pytest tests/agent -q => 123 passed
files_modified = 1
fixture_design_result = PASS
dry_run_design_result = PASS
evidence_index_validation_design_result = PASS
review_queue_validation_design_result = PASS
readiness_gates = CLOSED
```

R7AC 设计结论：

```text
fixture format = in-test SourceTextEvidence objects first
future file-backed fixtures = tests/agent/fixtures/source_text_sidecars/
dry-run path = lower-level helpers first，不跑 run_pilot 或真实 workbook rerun
evidence_index validation = tempfile
review_queue validation = in-memory
full source_text 不默认序列化
```

## 当前任务

```text
348N-R7AD source_text sidecar fixture dry-run implementation
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AD 要用受控 fixture dry-run 验证 R7AB source_text wiring，必须避免真实 rerun、full text 泄漏、VERIFIED 越界和 readiness 误开。
```

任务文档：

```text
docs/codex_tasks/348N_R7AD_source_text_sidecar_fixture_dry_run_implementation.md
```

当前任务类型：implementation + tests，优先 tests-only。

目标：使用 in-test SourceTextEvidence objects 做 fixture-first dry-run 覆盖，验证 evidence_index metadata、review_queue compact fields、positive/negative source_text selection cases、full source_text 不序列化、readiness gates 关闭。

## 当前推荐下一步

```text
执行 348N-R7AD source_text sidecar fixture dry-run implementation
```

R7AD 完成后应先做 R7AD-QA，再考虑真实 workbook dry-run 或 file-backed fixture loader。
