# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
VERIFIED 不等于 STRONG_EVIDENCE
VERIFIED 不等于 clean_data 自动放行
```

## 最新完成

```text
R7BI-QA review_queue writer dry-run schema alignment test-only contract prototype review completed
commit = ebe0dc9 docs: add R7BI QA review
Decision = PASS，R7BI test-only schema alignment contract QA approved
py_compile = PASS
schema alignment tests = 29 passed
dry-run integration tests = 36 passed
writer contract tests = 24 passed
adapter skeleton tests = 75 passed
full tests/agent = 404 passed
boundary_check = PASS
readiness_gates = CLOSED
```

R7BI-QA 结论：review_queue dry-run 链路已经具备 test-only integration boundary 和 test-only schema alignment contract，可以安全地把已验证的 dry-run writer preview 映射成未来 `review_queue` record preview 形状。它仍然没有生产 hook、没有真实数据库写入、没有文件输出、没有 migration、没有 export，也没有打开 readiness gates。

## 当前 review_queue dry-run 安全链路

```text
adapter candidate output
-> test-only dry-run integration boundary
-> test-only writer dry-run preview
-> test-only schema alignment contract
-> future review_queue record preview
```

这条链路证明：

```text
字段分类可审计
future preview metadata-first
required fields fail closed
forbidden fields fail closed
idempotency key deterministic
bounded evidence_preview allowed
full source_text forbidden
raw MinerU / raw Excel / raw parser / raw LLM-VLM payloads forbidden
clean_data intent / delivery export intent / production writer config forbidden
```

## 仍未完成

```text
真实 review_queue persistence 尚未实现
DB model / repository / migration 尚未实现
production writer 尚未实现
runner / CLI / production pipeline hook 尚未接入
clean_data 自动写入仍然关闭
formal delivery/export 仍然关闭
```

## 当前任务

```text
348N-R7BJ project documentation sync after review_queue dry-run schema alignment milestone
```

任务文档：

```text
docs/codex_tasks/348N_R7BJ_project_documentation_sync_after_review_queue_dry_run_schema_alignment_milestone.md
```

任务类型：docs-sync-only。

目标：把 R7BE 到 R7BI-QA 完成的 review_queue dry-run 安全链路、字段闸门、测试结果、仍未生产化的边界同步到项目主文档，并明确下一步应先做 R7BJ-QA 文档同步审查。
