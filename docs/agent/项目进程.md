# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 已验证 workbook families

```text
Anjing / 安井：R7U clean_data=65, review_queue=17, market_reference=10, R7S removed normal facts=0
Linyang / 林洋：R7U clean_data=0, review_queue=489, unknown=0, market_reference=10
Taihao / 泰豪：R7T clean_data 92 -> 72, review_queue 66 -> 86, 20 risky scaffolding rows moved to review_queue
```

## 历史摘要

### R1-R6：林洋 schema / qualitative_facts / guardrails

```text
R1-R3: normalized_testset / supporting / market reference sheet families 被显式识别，unknown_row_count 收敛到 0
R5: qualitative_facts 中文表头识别修复，clean_data_row_count 33 -> 0
R6B/R6B-FIX: output_schema_guardrails + logical/physical CSV count guardrail 成为 runner contract
```

### R7P-R7V：clean-boundary 收束

```text
R7P: Taihao pilot 暴露 MARKET_REFERENCE_ROW clean_data 泄漏
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7Q: 暴露 STRICT_FINANCIAL_TABLE_ROW pseudo-header / comparison-row clean-boundary 风险
R7S: 实现 scaffolding guard；tests/agent = 86 passed
R7T: Taihao rerun，clean 92 -> 72，review 66 -> 86
R7U: Linyang / Anjing 回归验证通过，无 R7S 回归
R7V: 跨 family clean-boundary validated，但 readiness gates 继续关闭
```

### R7W-R7Z-QA：evidence strengthening

```text
R7W: 设计 WEAK_EVIDENCE -> STRONG_EVIDENCE 路径
R7X: 实现 page_number parsing + agreement_status 字段
R7X-QA: 确认 page_number parsed 不等于 VERIFIED；UNVERIFIED 不等于 STRONG_EVIDENCE
R7Y: 实现 deterministic source-value agreement checker
R7Y-QA: 确认 VERIFIED / DISAGREED 语义有效且 readiness gates 关闭
R7Z: 修复 duplicate numeric values 的 VERIFIED false positive，改为 multiplicity-aware matching
R7Z-QA: 确认 multiplicity-aware agreement checker 有效，DISAGREED 保守，readiness gates 关闭
```

R7Z-QA 结果：

```text
commit = 898fa24 docs: add R7Z QA review
Decision = PASS，R7Z-QA confirms multiplicity-aware agreement checker is valid
build_result = PASS，py_compile 全部通过
test_result = PASS，pytest tests/agent -q => 111 passed
qa_result = VALID
verified_false_positive_result = REDUCED
disagreed_status_result = PASS
source_text_integration_result = NOT_CHANGED
readiness_gates = CLOSED
```

R7Z-QA 保留的下一阶段限制：

```text
row-level matching is still not period-aware or coordinate-aware
source_text 还没有接入真实 pipeline
```

## 当前任务

```text
348N-R7AA source_text integration design / evidence index wiring
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AA 要设计真实 source_text 如何进入 evidence agreement pipeline；如果设计错误，会造成后续 evidence_strength 判断不可靠。
```

任务文档：

```text
docs/codex_tasks/348N_R7AA_source_text_integration_design_evidence_index_wiring.md
```

当前任务类型：design / review task，不是实现任务。

目标：设计 source_text 的最小安全接入契约，明确 source_text 来源、provenance 绑定、evidence_index / review_queue 输出方式、缺失或不可信时的保守行为，以及下一步实现任务边界。

## 当前推荐下一步

```text
执行 348N-R7AA source_text integration design / evidence index wiring
```

R7AA 是设计任务。不要直接改代码接 pipeline，不要运行 MinerU/OCR/LLM/VLM，不要打开 readiness gates。
