# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
R7AE: file-backed source_text sidecar loader design completed
R7AF: test-only file-backed source_text sidecar loader implemented
R7AF-QA: file-backed source_text sidecar loader QA valid
R7AG: lightweight PDF evidence bridge design valid
R7AH: lightweight PDF evidence bridge test-only prototype implemented
R7AH-QA: lightweight PDF evidence bridge prototype QA valid
R7AI: real PDF text-layer provider design valid
```

R7AI 结果：

```text
commit = acd68dc docs: add R7AI real PDF text layer provider design
Decision = PASS，348N_R7AI_REAL_PDF_TEXT_LAYER_PROVIDER_DESIGN_VALID
test_result = PASS，pytest tests/agent -q => 180 passed
files_modified = 1
provider_design_result = PASS
dependency_strategy_result = PASS，先做 R7AJ dependency audit
cost_control_result = PASS
cache_design_result = PASS
fallback_policy_result = PASS
readiness_gates = CLOSED
```

R7AI 关键设计：

```text
real PDF text-layer provider remains bounded
optional-dependency-first
cache-manifested
metadata-only
non-promotional
no real PDF / MinerU / OCR / LLM / VLM / run_pilot / workbook rerun
```

## 当前任务

```text
348N-R7AJ dependency audit for real PDF text-layer provider
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = dependency-audit-only
```

任务文档：

```text
docs/codex_tasks/348N_R7AJ_dependency_audit_for_real_pdf_text_layer_provider.md
```

当前任务类型：dependency audit / design review task。

目标：只盘点当前依赖和配置，判断下一步真实 PDF text-layer provider 是否能用已有依赖安全实现，还是必须先做 optional dependency addition design 或 adapter-boundary-only skeleton。不得新增依赖，不得实现 PDF parser，不得跑真实 PDF。

## 当前推荐下一步

```text
执行 348N-R7AJ dependency audit for real PDF text-layer provider
```

R7AJ 通过后，根据依赖盘点结果选择 R7AK：test-only provider implementation、optional dependency addition design，或 adapter-boundary-only provider skeleton。不要直接跳到真实 PDF 批量运行、MinerU-first 或 production readiness。
