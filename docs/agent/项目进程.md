# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
R7AE: file-backed source_text sidecar loader design completed
R7AF: test-only file-backed source_text sidecar loader implemented
R7AF-QA: file-backed source_text sidecar loader QA valid
R7AG: lightweight PDF evidence bridge design valid
R7AH: lightweight PDF evidence bridge test-only prototype implemented
R7AH-QA: lightweight PDF evidence bridge prototype QA valid
```

R7AH-QA 结果：

```text
commit = d0529a7 docs: add R7AH QA review
Decision = PASS; 348N_R7AH_QA_CONFIRMED_LIGHTWEIGHT_PDF_EVIDENCE_BRIDGE_PROTOTYPE_VALID
test_result = PASS; pytest tests/agent -q => 180 passed
files_modified = 1
qa_result = VALID
readiness_gates = CLOSED
```

R7AH-QA 确认：

```text
prototype remains test-only under tests/agent/
synthetic page-text provider only
no real PDF IO
no PyMuPDF/pdfplumber/pypdf imports
no new dependencies
no MinerU/OCR/LLM/VLM
no production hook
value + metric + period proximity required
conservative failures remain conservative
SourceTextEvidence mapping valid
evidence_index metadata-only
review_queue compact-only
full snippet text not serialized
VERIFIED does not affect STRONG_EVIDENCE, clean admission, or readiness
```

## 当前任务

```text
348N-R7AI real PDF text-layer provider design
```

任务大小与执行方式：

```text
task_size = small
recommended_reasoning_level = max
execution_mode = design-review-only
```

任务文档：

```text
docs/codex_tasks/348N_R7AI_real_pdf_text_layer_provider_design.md
```

当前任务类型：design / review task。

目标：设计从 synthetic page-text provider 走向真实 PDF text-layer provider 的第一步。只做设计，不实现、不新增依赖、不跑真实 PDF、不接 MinerU/OCR/LLM/VLM；重点是 provider boundary、依赖策略、文本层检测、页选择/成本控制、缓存 manifest、fallback policy 和后续 R7AJ 切片。

## 当前推荐下一步

```text
执行 348N-R7AI real PDF text-layer provider design
```

R7AI 通过后，再根据依赖审查结果决定 R7AJ 是 test-only implementation 还是 dependency audit。不要直接跳到真实 PDF 批量运行、MinerU-first 或 production readiness。
