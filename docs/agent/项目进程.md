# DateFac Agent 项目进程

> 当前主线：`DateFac Agent`，围绕金融文档 AI 抽取结果做审计、复核队列、证据约束和内部 clean-data candidate 输出。
>
> 当前工作区：`D:\_datefac_agent`
>
> 当前分支：`pivot/348-agent-foundation`
>
> 当前流程：`intake -> audit -> review -> delivery`

---

## 0. 当前工程约束

```text
不跑 MinerU
不调用 LLM/VLM
不 OCR
不重新抽 PDF
不覆盖历史 output
不触碰旧 D:\_datefac 工作区
不触碰 legacy datefac/ 主包
不提交 output 文件
不声明 client_ready
不声明 production_ready
不打开 formal_client_export_allowed
```

---

## 1. 已完成阶段概要

### 348A：第一真实 workbook 审计链路跑通

```text
PDF:   H3_AP202606081823352906_1_331fresh_20260615_21591.pdf
Excel: 安井食品研报数据汇总.xlsx
```

初始链路可跑通，但 evidence policy 和 row-type policy 过粗。

### 348A-R1：Evidence Policy Refinement

```text
missing_evidence_count: 82 -> 0
weak_evidence_count = 82
```

将 workbook lineage 从真正 missing evidence 中拆开。

### 348A-R2：Row Type Classification Refinement

新增并验证：

```text
STRICT_FINANCIAL_TABLE_ROW
MARKET_REFERENCE_ROW
NARRATIVE_ASSERTION
UNKNOWN_ROW
```

第一样本：

```text
strict_financial_table_row_count = 67
market_reference_row_count = 10
narrative_assertion_count = 5
unknown_row_count = 0
```

### 348A-R3：Unit Checker False Positive Refinement

修复 `净资产收益率(%)` 被误判为 `monetary_unit_mismatch`。

```text
unit_issue_count: 1 -> 0
fail_count: 1 -> 0
```

### 348A-R4：Clean Data Candidate Policy

内部 clean-data candidate 规则形成：

```text
STRICT_FINANCIAL_TABLE_ROW + WEAK_EVIDENCE + 无 unit/period/valuation issue
=> INTERNAL_CLEAN_CANDIDATE

MARKET_REFERENCE_ROW + WEAK_EVIDENCE + 无 unit issue
=> INTERNAL_REFERENCE_CANDIDATE

NARRATIVE_ASSERTION
=> NARRATIVE_REVIEW，不进 clean_data
```

第一样本：

```text
clean_data_row_count = 75
review_queue_row_count = 7
client_ready = false
production_ready = false
formal_client_export_allowed = false
```

### 348A-R4-QA

确认 clean-data 中无 narrative、无 period issue、无 unit/valuation issue。

---

## 2. 第二真实 workbook 泛化阶段

### 348S：Second Real Workbook Pilot

```text
PDF:   H3_AP202605231822706325_1.pdf
Excel: H3_AP202605231822706325_1_提取结果.xlsx
```

初次结果暴露 schema/intake 泛化缺口：

```text
row_count_total = 119
fail_count = 2
unit_issue_count = 2
period_issue_count = 66
unknown_row_count = 53
clean_data_row_count = 0
review_queue_row_count = 119
```

### 348S-R1：Intake Schema Generalization

改进方向：

```text
新增 header-row detection
新增 key-value summary sheet 支持
扩展 strict / market / narrative sheet mapping
增强 period label search
```

第二样本改善：

```text
row_count_total: 119 -> 112
unknown_row_count: 53 -> 0
period_issue_count: 66 -> 5
clean_data_row_count: 0 -> 87
review_queue_row_count: 119 -> 25
```

第一样本 baseline 未明显回退：

```text
clean_data_row_count = 75
review_queue_row_count = 7
```

### 348S-R1-QA

确认 `119 -> 112` 的 7 行差异来自 header-like pollution rows 被正确剔除。

剩余问题：

```text
5 个 period issue：盈利预测分业务 embedded period header 未识别
2 个 unit issue：资产负债率(%) / 资产负债率(%,LF) 被误判为 monetary metric
```

### 348S-R2：Unit/Period Residual Refinement

修复方向：

```text
资产负债率 / 负债率 识别为 rate metric
不全局移除 资产 / 负债 的金额语义
识别 embedded period header
```

第二样本结果：

```text
row_count_total = 112
fail_count = 0
unit_issue_count = 0
period_issue_count = 0
clean_data_row_count = 94
review_queue_row_count = 18
unknown_row_count = 0
```

### 348S-R2-QA

结论：

```text
348S_R2_QA_CONFIRMED_RESIDUAL_REFINEMENT_VALID
```

确认 2 个 unit residual 与 5 个 period residual 不是被删除，而是转为 clean candidate。

---

## 3. Fixture 阶段

### 348F：Fixture Harvest from 346B

新增 fixture 文件：

```text
tests/agent/fixtures/unit_semantics__346b_lessons_and_348s_r2__v1.json
tests/agent/fixtures/period_detection__embedded_headers_and_missing_period__v1.json
tests/agent/fixtures/routing_policy__narrative_market_strict_and_missing_evidence__v1.json
```

新增测试：

```text
test_unit_semantic_fixture_cases
test_period_detection_fixture_cases
test_clean_candidate_routing_fixture_cases
```

验证：

```text
pytest passed: 29 passed
LLM / MinerU / OCR = 0
```

### 348F-QA

结论：

```text
348F_QA_CONFIRMED_FIXTURE_HARVEST_VALID
```

确认 fixture 小型、语义清晰，测试调用真实 checker / policy。

---

## 4. 第三真实 workbook 阶段

### 348S Third Workbook Pilot

第三样本配对已确认：

```text
PDF:   H3_AP202605231822706325_1.pdf
Excel: 泰豪科技_深度研报_核心数据提取_豆包AI生成 (1).xlsx
```

配对理由：PDF 首页含 `泰豪科技（600590）`，Excel 文件名含 `泰豪科技`。

首次 runner 失败：

```text
failure type: IndexError: list index out of range
failure stage: intake
failure location: datefac_agent/intake/excel_intake.py
failure trigger: _find_key_value_start() assumed at least two text cells in a row
```

结论：

```text
348S_THIRD_WORKBOOK_PILOT_CONFIRMED_NEEDS_SCHEMA_REFINEMENT
```

### 348S-R3：Targeted Third Workbook Schema Refinement

已完成窄修：

```text
_find_key_value_start() 对空行 / 单列 summary 行增加防御判断
短行不再触发 IndexError
两列 key-value 行仍可正常识别
```

新增回归测试：

```text
test_find_key_value_start_ignores_short_summary_rows_without_crashing
```

验证：

```text
py_compile passed
pytest passed: 30 passed
第三 workbook runner 不再 IndexError
```

R3 后第三 workbook runner 可完成，但产出为空：

```text
decision = AI_EXCEL_INTAKE_AUDIT_348A_READY
sheet_count = 8
row_count_total = 0
row_count_audited = 0
clean_data_row_count = 0
review_queue_row_count = 0
```

当前 blocker：

```text
阶段：intake interpretation
症状：runner 成功结束，但所有 sheet 被 intake 成 0 rows
含义：crash 已修，仍需处理 one-column summary / later-tabular workbook 的结构解释
```

结论：

```text
348S_R3_CONFIRMED_PARTIAL_FIX_STILL_BLOCKED
```

推荐下一步：

```text
348S-R3B Third Workbook Zero-Row Intake Follow-up
```

---

## 5. 当前阶段总结

关键结果：

```text
第一样本：clean_data_row_count = 75，review_queue_row_count = 7
第二样本 R1：clean_data_row_count = 87，review_queue_row_count = 25
第二样本 R2：clean_data_row_count = 94，review_queue_row_count = 18
fixture-backed tests: 29 passed -> 30 passed
第三样本 R3：crash fixed, but row_count_total = 0
```

当前仍保持：

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
LLM / MinerU / OCR = 0
```

下一步：

```text
348S-R3B Third Workbook Zero-Row Intake Follow-up
```
