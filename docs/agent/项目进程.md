# DateFac Agent 项目进程

> 当前主线：`DateFac Agent`，围绕金融文档 AI 抽取结果做审计、复核队列、证据约束和内部 clean-data candidate 输出。
>
> 当前工作区：`D:\_datefac_agent`
>
> 当前分支：`pivot/348-agent-foundation`
>
> 当前流程：`intake -> audit -> review -> delivery`

---

## 0. 当前工程约束

默认原则：

```text
不跑 MinerU
不调用 LLM/VLM
不 OCR
不重新抽 PDF
不覆盖历史 output
不触碰旧 D:\_datefac 工作区
不触碰 legacy datefac/ 主包
不提交 output 文件
不声明 client_ready
不声明 production_ready
不打开 formal_client_export_allowed
```

---

## 1. 348A：第一真实 workbook 审计链路跑通

样本：

```text
PDF:   H3_AP202606081823352906_1_331fresh_20260615_21591.pdf
Excel: 安井食品研报数据汇总.xlsx
```

初始结果：

```text
row_count_total = 82
review_count = 81
fail_count = 1
missing_evidence = 82
clean_data_row_count = 0
```

结论：链路能跑通，但 evidence policy 和 row-type policy 过粗。

---

## 2. 348A-R1：Evidence Policy Refinement

将 workbook lineage 从 `MISSING_EVIDENCE` 中拆出，形成 evidence level：

```text
STRONG_EVIDENCE
WEAK_EVIDENCE
MISSING_EVIDENCE
NOT_APPLICABLE
```

结果：

```text
missing_evidence_count: 82 -> 0
weak_evidence_count = 82
```

结论：弱证据和真正缺证据被区分开。

---

## 3. 348A-R2：Row Type Classification Refinement

新增 row type：

```text
STRICT_FINANCIAL_TABLE_ROW
MARKET_REFERENCE_ROW
NARRATIVE_ASSERTION
UNKNOWN_ROW
```

第一真实样本结果：

```text
strict_financial_table_row_count = 67
market_reference_row_count = 10
narrative_assertion_count = 5
unknown_row_count = 0
```

结论：严格财务表、市场参考行、叙事观点行被拆开。

---

## 4. 348A-R3：Unit Checker False Positive Refinement

问题：`净资产收益率(%)` 被误判为 `monetary_unit_mismatch`。

修复方向：将 ROE / 净资产收益率 / 收益率 / 毛利率 / 净利率等识别为 percentage/rate metric。

结果：

```text
unit_issue_count: 1 -> 0
fail_count: 1 -> 0
review_count = 82
```

结论：第一样本明确 unit false positive 修复，同时保留真实金额/百分比错配检查。

---

## 5. 348A-R4：Clean Data Candidate Policy

核心原则：

```text
internal clean-data candidate ≠ client_ready
internal clean-data candidate ≠ production_ready
internal clean-data candidate ≠ formal client delivery
```

主要规则：

```text
STRICT_FINANCIAL_TABLE_ROW + WEAK_EVIDENCE + 无 unit/period/valuation issue
=> INTERNAL_CLEAN_CANDIDATE

MARKET_REFERENCE_ROW + WEAK_EVIDENCE + 无 unit issue
=> INTERNAL_REFERENCE_CANDIDATE

NARRATIVE_ASSERTION
=> NARRATIVE_REVIEW，不进 clean_data
```

结果：

```text
clean_data_row_count = 75
review_queue_row_count = 7
internal_clean_candidate_count = 65
internal_reference_candidate_count = 10
narrative_review_count = 5
review_required_count = 2
client_ready = false
production_ready = false
formal_client_export_allowed = false
```

结论：第一真实样本产生内部可用 clean-data candidate，但没有打开正式交付 gate。

---

## 6. 348A-R4-QA：Clean Data Candidate Policy Review

QA 结果：

```text
clean_data_row_count = 75
review_queue_row_count = 7
clean_data 中无 narrative
clean_data 中无 period issue
clean_data 中无 unit/valuation issue
client_ready = false
production_ready = false
formal_client_export_allowed = false
```

结论：R4 clean candidate policy 在第一真实样本上有效。

---

## 7. 348S：Second Real Workbook Pilot

第二样本：

```text
PDF:   H3_AP202605231822706325_1.pdf
Excel: H3_AP202605231822706325_1_提取结果.xlsx
```

跳过样本：

```text
泰豪科技_深度研报_核心数据提取_豆包AI生成 (1).xlsx
```

跳过原因：未找到明确对应 source PDF。

348S 初次结果：

```text
row_count_total = 119
fail_count = 2
unit_issue_count = 2
period_issue_count = 66
strict_financial_table_row_count = 66
market_reference_row_count = 0
narrative_assertion_count = 0
unknown_row_count = 53
clean_data_row_count = 0
review_queue_row_count = 119
```

结论：runner 没崩，但第二 workbook 暴露明显 schema/intake 泛化缺口。

---

## 8. 348S-R1：Intake Schema Generalization

修改方向：

```text
新增 header-row detection
新增 key-value summary sheet 支持
扩展 strict / market / narrative sheet mapping
增强 period label search
```

第二样本改善结果：

```text
row_count_total: 119 -> 112
unknown_row_count: 53 -> 0
period_issue_count: 66 -> 5
clean_data_row_count: 0 -> 87
review_queue_row_count: 119 -> 25
internal_clean_candidate_count = 75
internal_reference_candidate_count = 12
narrative_review_count = 18
review_required_count = 5
excluded_from_clean_data_count = 2
```

baseline 回归结果：

```text
row_count_total = 82
unit_issue_count = 0
period_issue_count = 2
unknown_row_count = 0
clean_data_row_count = 75
review_queue_row_count = 7
```

结论：R1 显著改善第二 workbook 泛化，同时第一样本 baseline 未明显回退。

---

## 9. 348S-R1-QA：Intake Generalization Review

QA 结论：`119 -> 112` 的 7 行差异合理，来自 7 个结构化 sheet 各去掉 1 行 header-like pollution row。

剩余问题：

```text
5 个 period issue：盈利预测分业务 的 embedded period header 未识别
2 个 unit issue：资产负债率(%) / 资产负债率(%,LF) 被误判为 monetary metric
```

结论：R1-QA 确认 intake generalization 是有效改善。

---

## 10. 348S-R2：Unit/Period Residual Refinement

修复方向：

```text
将 资产负债率 / 负债率 识别为明确的 rate metric
不全局移除 资产 / 负债 的金额语义
识别 embedded period header：2025A收入(亿元)、2026E收入(亿元)、2027E收入(亿元)、2028E收入(亿元)、2026E毛利率(%)
```

已提交执行结果：

```text
row_count_total = 112
fail_count = 0
unit_issue_count = 0
period_issue_count = 0
clean_data_row_count = 94
review_queue_row_count = 18
unknown_row_count = 0
```

baseline regression：

```text
row_count_total = 82
fail_count = 0
unit_issue_count = 0
period_issue_count = 2
clean_data_row_count = 75
review_queue_row_count = 7
unknown_row_count = 0
```

结论：R2 已提交，残留 7 行被收敛。

---

## 11. 348S-R2-QA：Unit/Period Residual Refinement Review

QA 结论：

```text
348S_R2_QA_CONFIRMED_RESIDUAL_REFINEMENT_VALID
```

第二样本 R2 verified metrics：

```text
row_count_total = 112
row_count_audited = 112
fail_count = 0
unit_issue_count = 0
period_issue_count = 0
clean_data_row_count = 94
review_queue_row_count = 18
strict_financial_table_row_count = 81
market_reference_row_count = 13
narrative_assertion_count = 18
unknown_row_count = 0
internal_clean_candidate_count = 81
internal_reference_candidate_count = 13
narrative_review_count = 18
review_required_count = 0
excluded_from_clean_data_count = 0
```

修复验证：

```text
2 个 unit residual 行仍存在于 clean_data/evidence_index，不再带 monetary_unit_mismatch
5 个 period residual 行仍存在于 clean_data/evidence_index，不再带 period_context_missing
clean_data 94 行全部只有 weak_evidence，无 unit/period/valuation issue 泄漏
review_queue 18 行全部是 NARRATIVE_ASSERTION / NARRATIVE_REVIEW
```

结论：R2-QA 通过。推荐进入 fixture harvest。

---

## 12. 348F：Fixture Harvest from 346B

目标：从历史 `346B` 语义教训、348A-R4 clean candidate policy、348S-R2-QA 已验证修复中，提炼小型、稳定、可重复运行的 agent audit fixtures。

创建 fixture 文件：

```text
tests/agent/fixtures/unit_semantics__346b_lessons_and_348s_r2__v1.json
tests/agent/fixtures/period_detection__embedded_headers_and_missing_period__v1.json
tests/agent/fixtures/routing_policy__narrative_market_strict_and_missing_evidence__v1.json
```

新增 fixture-backed 测试：

```text
test_unit_semantic_fixture_cases
test_period_detection_fixture_cases
test_clean_candidate_routing_fixture_cases
```

验证：

```text
py_compile passed
pytest passed: 29 passed
未提交 output 文件
未触碰 legacy datefac/
LLM / MinerU / OCR = 0
```

结论：

```text
348F_CONFIRMED_FIXTURE_HARVEST_COMPLETE
```

---

## 13. 348F-QA：Fixture Harvest Review

QA 结论：

```text
348F_QA_CONFIRMED_FIXTURE_HARVEST_VALID
```

fixture 结构结论：

```text
3 个 fixture 都是小型 JSON
不依赖 input/ output/ temp/ data/
没有复制大输出快照
包含 source_task / issue_type / why_it_matters / case_id 等 traceability 字段
```

覆盖与测试质量：

```text
unit fixture 覆盖资产负债率、资产总计、负债合计、净资产收益率、EPS guardrail
period fixture 覆盖 embedded period、FY2026 / 2027FY / 2028 Q1、真正无 period row
routing fixture 覆盖 narrative、market reference、strict clean、period issue、missing evidence
测试调用真实 audit_unit_semantics / detect_period_labels / audit_period_alignment / audit_evidence_presence / build_row_audit_result / build_review_queue_rows
```

验证与边界：

```text
py_compile passed
pytest passed: 29 passed
未修改 datefac_agent/ 源码
未新增 fixture 类别
未提交 output
未触碰 legacy datefac/
未跑 MinerU / LLM / OCR
未重新抽 PDF
```

结论：fixture harvest 通过 QA。

---

## 14. 348S Third Workbook Pilot

目标：第三份真实 workbook 泛化验证。

候选清单：

```text
PDF:
- H3_AP202605231822706325_1.pdf
- H3_AP202606081823352906_1_331fresh_20260615_21591.pdf

Excel:
- H3_AP202605231822706325_1_提取结果.xlsx
- 安井食品研报数据汇总.xlsx
- 泰豪科技_深度研报_核心数据提取_豆包AI生成 (1).xlsx
```

执行结果：

```text
未选择第三组
未创建 output 目录
未运行 runner
```

阻塞原因：前两份 PDF 已分别对应第一、第二样本；剩余 `泰豪科技_深度研报_核心数据提取_豆包AI生成 (1).xlsx` 没有可证明匹配的 source PDF。

验证：

```text
py_compile passed
pytest passed: 29 passed
未修改源码
未提交 output
LLM / MinerU / OCR = 0
```

结论：

```text
348S_THIRD_WORKBOOK_PILOT_BLOCKED_NO_MATCHED_INPUT_PAIR
```

推荐下一步：

```text
348S-Input Pairing Manifest
```

---

## 15. 当前阶段总结

当前链路已经从单样本 pilot 进入双样本泛化验证，并开始把已验证行为沉淀为 fixture。

关键进展：

```text
第一样本：clean_data_row_count = 75，review_queue_row_count = 7
第二样本 R1：clean_data_row_count = 87，review_queue_row_count = 25
第二样本 R2：clean_data_row_count = 94，review_queue_row_count = 18
fixture-backed tests: 29 passed
第三样本：blocked by no matched input pair
```

当前仍然保持：

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
llm_api_call_count = 0
mineru_run_count = 0
ocr_run_count = 0
```

下一步推荐：

```text
348S-Input Pairing Manifest
```

如果用户补充泰豪科技 source PDF，则可以重启：

```text
348S Third Workbook Pilot
```
