# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
VERIFIED 不等于 STRONG_EVIDENCE
VERIFIED 不等于 clean_data 自动放行
```

## 最新完成

```text
R7AO test-only MinerU adapter controlled comparison runner completed locally
Decision = PASS，348N_R7AO_LOCAL_CONTROLLED_COMPARISON_COMPLETED
pytest tests/agent/test_mineru_artifact_adapter_348n.py -q = 21 passed
pytest tests/agent -q = 201 passed
runner_result = PASS
comparison_result = PASS，451 rows compared
DateFac candidate rows = 451
MinerU adapter blocks = 89
VERIFIED = 402
review_required = 49
DISAGREED = 5
AMBIGUOUS = 10
MISSING_EVIDENCE = 1
PARSE_SKIPPED = 18
probe_examples_result = VERIFIED:11
boundary_check = PASS
readiness_gates = CLOSED
commit = none
push = none
```

R7AO 本地输出目录：

```text
D:\_datefac_agent\output\comparison\anjing_foods_mineru_adapter_r7ao\
```

R7AO 说明：这是 no-commit local dry-run。它用完整安井 DateFac Excel、完整本地 MinerU `content_list_v2` 和 R7AM test-only adapter 执行了 controlled comparison，生成本地 xlsx/csv/md/json 报告，但没有修改生产代码、没有提交 output、没有打开 readiness gates。

## 当前任务

```text
348N-R7AO-QA test-only MinerU adapter controlled comparison runner review
```

任务文档：

```text
docs/codex_tasks/348N_R7AO_QA_test_only_mineru_adapter_controlled_comparison_runner_review.md
```

任务类型：QA-review-only。

目标：审查 R7AO 的本地 runner 和输出报告，确认计数一致性、探针覆盖、边界安全、没有 tracked contamination，并决定下一步是生产边界设计、多文档 dry-run 设计，还是 discrepancy review workflow 设计。
