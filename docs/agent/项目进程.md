# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7S: strict-table scaffolding guard
R7V: cross-family clean-boundary validated，readiness gates closed
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: 确认 source_text wiring 有效，metadata/hash only，readiness gates closed
```

R7AB-QA 结果：

```text
commit = 758ec98 docs: add R7AB QA review
Decision = PASS，R7AB-QA confirms source_text wiring valid
test_result = PASS，pytest tests/agent -q => 123 passed
files_modified = 1
source_text_contract_result = PASS
source_text_selection_result = PASS
agreement_injection_result = PASS
evidence_index_wiring_result = PASS
review_queue_wiring_result = PASS
full_text_serialization_result = PASS，no full source_text
qa_result = VALID
readiness_gates = CLOSED
```

R7AB-QA 关键边界：

```text
source_text missing / untrusted / mismatch / empty -> UNVERIFIED
Excel raw fields are not trusted source_text
evidence_index and review_queue do not serialize full source_text
VERIFIED 不等于 STRONG_EVIDENCE / clean admission / production_ready
```

## 当前任务

```text
348N-R7AC source_text sidecar fixture integration / dry-run design
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AC 要设计如何用 fixture sidecar / dry-run 安全测试 R7AB source_text wiring，避免真实 pipeline 接入前出现 VERIFIED 误判或边界外泄。
```

任务文档：

```text
docs/codex_tasks/348N_R7AC_source_text_sidecar_fixture_integration_dry_run_design.md
```

当前任务类型：design / review task，不是实现任务。

目标：设计 source_text sidecar fixture / dry-run 的最小安全路径，明确 fixture 格式、位置、positive/negative cases、evidence_index 与 review_queue 验证方式；不运行 workbook rerun，不接 MinerU/OCR/LLM/VLM，不打开 readiness gates。

## 当前推荐下一步

```text
执行 348N-R7AC source_text sidecar fixture integration / dry-run design
```

R7AC 通过后，再考虑 R7AD fixture sidecar dry-run implementation。不要直接跳到真实 workbook rerun 或 production readiness。
