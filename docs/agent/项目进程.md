# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
R7AD-QA: fixture dry-run QA valid
R7AE: file-backed source_text sidecar loader design completed
```

R7AE 结果：

```text
commit = d24abf9 docs: add R7AE source text loader design
Decision = PASS，R7AE source_text file-backed sidecar loader design completed
test_result = PASS，pytest tests/agent -q => 134 passed
files_modified = 1
sidecar_format_result = PASS，JSON object v1
loader_design_result = PASS，test-only first
fail_closed_design_result = PASS
evidence_index_review_queue_design_result = PASS，no full source_text
readiness_gates = CLOSED
```

R7AE 设计结论：

```text
sidecar format = JSON object v1, not JSONL
schema = schema_version + fixture_scope=test_only + records[]
strict required fields, no unknown keys
fixture location = tests/agent/fixtures/source_text_sidecars/
loader placement = test-only first
malformed/unsafe/hash mismatch/duplicate/unsupported fields reject with no partial records
text_sha256 = recompute UTF-8 SHA-256
full text remains test-only and must not serialize to evidence_index/review_queue
```

## 当前任务

```text
348N-R7AF test-only source_text file-backed sidecar loader implementation
```

任务大小与执行方式：

```text
task_size = large
recommended_reasoning_level = max
execution_mode = phased-hard-stop + implementation-with-self-QA
```

任务文档：

```text
docs/codex_tasks/348N_R7AF_test_only_source_text_file_backed_sidecar_loader_implementation.md
```

当前任务类型：implementation + tests + self-QA。

目标：实现 test-only JSON sidecar loader、提交测试 fixture、覆盖 valid loading / fail-closed / evidence_index metadata / review_queue compact fields / full source_text 不序列化，不接 production path，不跑 workbook rerun。

## 当前推荐下一步

```text
执行 348N-R7AF test-only source_text file-backed sidecar loader implementation
```

R7AF 通过后通常做 R7AF-QA。不要直接跳到真实 workbook rerun 或 production readiness。
