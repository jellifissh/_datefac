# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
```

## 最新完成

```text
R7AL Anjing DateFac-vs-MinerU controlled comparison dry-run completed locally
Decision = COMPLETED_CONTROLLED_COMPARISON_DRY_RUN
DateFac candidate rows normalized = 451
MinerU blocks indexed = 173
VERIFIED = 395
review_required_total = 56
DISAGREED = 10
AMBIGUOUS = 10
MISSING_EVIDENCE = 2
required 11 probe examples = all found and VERIFIED against MinerU v2 blocks
primary input recommendation = content_list_v2
content_list = fallback / cross-check
readiness_gates = CLOSED
commit = none; output files not committed
```

R7AL 说明：这是第一轮真实业务样本对比。DateFac Excel 候选 rows 与 MinerU `content_list_v2` evidence blocks 完成 controlled comparison，证明 MinerU 产物可作为 SourceTextEvidence 候选来源，但该结果仍不打开生产就绪门。

本地输出：

```text
D:\_datefac_agent\output\comparison\anjing_foods_datefac_vs_mineru\anjing_datefac_vs_mineru_comparison_report.xlsx
D:\_datefac_agent\output\comparison\anjing_foods_datefac_vs_mineru\anjing_datefac_vs_mineru_comparison_summary.md
D:\_datefac_agent\output\comparison\anjing_foods_datefac_vs_mineru\anjing_datefac_vs_mineru_evidence_rows.csv
D:\_datefac_agent\output\comparison\anjing_foods_datefac_vs_mineru\anjing_datefac_vs_mineru_unmatched_rows.csv
```

## 当前任务

```text
348N-R7AM test-only MinerU artifact adapter prototype
```

任务文档：

```text
docs/codex_tasks/348N_R7AM_test_only_mineru_artifact_adapter_prototype.md
```

任务类型：test-only implementation with self-QA。

目标：把 R7AL 证明可行的 MinerU artifact -> evidence block 形状，收敛成 `tests/agent/` 下的最小可重复 adapter prototype。只提交小型 curated fixture，不提交完整 MinerU output，不修改 production code，不接生产链路。
