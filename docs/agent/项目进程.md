# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前主线：对已有抽取结果做确定性审计，生成 `clean_data`、`review_queue`、`evidence_index`。核心原则仍然是宁可进入复核队列，也不要把不确定数据放进 clean_data。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
VERIFIED 不等于 STRONG_EVIDENCE
VERIFIED 不等于 clean_data 自动放行
```

## 最新完成

```text
R7AM-QA test-only MinerU artifact adapter prototype review completed
commit = 4daa3bf docs: add R7AM QA review
pytest tests/agent/test_mineru_artifact_adapter_348n.py -q = 21 passed
pytest tests/agent -q = 201 passed
files_modified = 1
fixture_review_result = PASS，小型 curated fixture，2599 bytes / 7 blocks
adapter_review_result = PASS，adapter 仅在 tests/agent/，无 production hook
matching_helper_review_result = PASS，保守区分 VERIFIED / UNVERIFIED / DISAGREED / AMBIGUOUS / MISSING_EVIDENCE
boundary_check = PASS
readiness_gates = CLOSED
```

R7AM-QA 结论：R7AM 的 MinerU adapter 原型保持 test-only；没有修改 production code；没有提交完整 MinerU output、DateFac Excel 或 comparison output；没有打开 readiness gates。

## 当前任务

```text
348N-R7AN test-only MinerU adapter controlled comparison dry-run design
```

任务文档：

```text
docs/codex_tasks/348N_R7AN_test_only_mineru_adapter_controlled_comparison_dry_run_design.md
```

任务类型：design-review-only。

目标：设计下一步如何用 R7AM 的 test-only adapter 形状执行 controlled comparison dry-run。R7AN 只做设计，不写 runner、不改 tests、不接生产、不提交 output。
