# DateFac Agent 项目进程

## 当前主线

```text
DateFac Agent
工作区：D:\_datefac_agent
分支：pivot/348-agent-foundation
流程：intake -> audit -> review -> delivery
active package：datefac_agent/
```

当前项目主线是对已有 Excel 抽取结果做确定性审计：识别 row type，检查 evidence / unit / period / valuation / clean admission，并把结果拆成 `clean_data` 与 `review_queue`。

## 当前边界

```text
client_ready = false
production_ready = false
formal_client_export_allowed = false
demo_export_only = true
LLM / MinerU / OCR / VLM = 0
output 文件默认不提交
legacy datefac/ 默认不修改
```

## 近期阶段摘要

```text
R7P-FIX2: MARKET_REFERENCE_ROW -> REVIEW_REQUIRED
R7S: strict-table scaffolding guard
R7V: cross-family clean-boundary validated，readiness gates closed
R7X: page_number parsing + agreement_status
R7Y: deterministic source-value agreement checker
R7Z: multiplicity-aware matching，降低重复数字 VERIFIED 误判
R7AA: source_text integration / evidence index wiring design
R7AB: source_text sidecar/index + trusted selection + evidence_index metadata wiring
R7AB-QA: source_text wiring QA valid
R7AC: source_text sidecar fixture / dry-run design completed
R7AD: source_text sidecar fixture dry-run coverage implemented
```

R7AD 结果：

```text
commit = 9cd4ef6 test: add source text fixture dry-run coverage
Decision = PASS，R7AD source_text fixture dry-run coverage implemented
test_result = PASS，pytest tests/agent -q => 134 passed
files_modified = 1
fixture_dry_run_result = PASS
evidence_index_validation_result = PASS
review_queue_validation_result = PASS
full_text_serialization_result = PASS
readiness_gates = CLOSED
```

R7AD 覆盖：

```text
trusted fixture verifies
trusted numeric mismatch -> DISAGREED
missing / source_id mismatch / page mismatch / locator mismatch / untrusted / empty -> UNVERIFIED
evidence_index tempfile metadata validation, no full source_text
review_queue in-memory compact fields validation, no full source_text
VERIFIED 不影响 STRONG_EVIDENCE / clean admission / readiness
```

## 当前任务

```text
348N-R7AD-QA source_text fixture dry-run review
```

建议思考程度：

```text
recommended_reasoning_level = max
reason = R7AD-QA 要确认 fixture dry-run 真实覆盖 source_text selection、evidence_index metadata、review_queue compact fields、全文不序列化和 clean/readiness 边界。
```

任务文档：

```text
docs/codex_tasks/348N_R7AD_QA_source_text_fixture_dry_run_review.md
```

当前任务类型：QA / review task。

目标：审查 R7AD 的 tests-only dry-run 是否有效覆盖 positive/negative cases，确认没有真实 workbook rerun、没有 full source_text 序列化、没有 readiness gate 或 clean admission 变化。

## 当前推荐下一步

```text
执行 348N-R7AD-QA source_text fixture dry-run review
```

R7AD-QA 通过后，再考虑真实 workbook dry-run design 或 file-backed source_text sidecar loader 设计。不要直接跳到 production readiness。
