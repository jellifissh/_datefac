# 348N-R1 Linyang Unknown Row Shape Diagnosis

## Goal

Diagnose why the new Linyang Energy workbook produced an unusually high unknown-row count.

This is a diagnosis task, not a code-fix task.

Do not modify source code, tests, input files, or output files.

---

## Current context

348N selected a new real PDF+Excel pair:

```text
PDF:   D:\_datefac_agent\input\6862e6f3995d3dbfbed310b51601fb0a.pdf
Excel: D:\_datefac_agent\input\linyang_energy_pdf_extracted_testset (1).xlsx
```

Pairing basis was safe:

```text
Excel README/source metadata references the same PDF filename
company metadata points to 林洋能源
no OCR / PDF re-extraction / LLM was used for pairing
```

348N runner output:

```text
D:\_datefac_agent\output\agent_excel_intake_audit_348n_linyang_energy_testset
```

348N result:

```text
row_count_total = 483
clean_data_row_count = 37
review_queue_row_count = 446
unknown_row_count = 367
unit_issue_count = 9
period_issue_count = 0
valuation_issue_count = 0
evidence_issue_count = 397
strong_evidence_count = 86
weak_evidence_count = 397
client_ready = false
production_ready = false
formal_client_export_allowed = false
llm_api_call_count = 0
mineru_run_count = 0
ocr_run_count = 0
```

Decision:

```text
348N_CONFIRMED_NEW_REAL_WORKBOOK_NEEDS_TARGETED_REFINEMENT
```

---

## Required context

Read:

```text
AGENTS.md
.skills/README.md
.skills/git_workflow.md
.skills/datefac_agent_foundation.md
.skills/agent_excel_intake_audit_workflow.md
docs/agent/项目进程.md
docs/project_handoffs/CURRENT_MODEL_HANDOFF.md
docs/agent/348N_NEW_REAL_WORKBOOK_GENERALIZATION_PILOT_RESULT.md
docs/agent/348A_R4_QA_CLEAN_DATA_CANDIDATE_POLICY_REVIEW.md
docs/agent/348S_R4_QA_STRICT_ROW_UNIT_PERIOD_REVIEW_SIGNAL_REFINEMENT_REVIEW.md
```

---

## Preflight

```powershell
cd D:\_datefac_agent
git pull --ff-only origin pivot/348-agent-foundation
git status -sb
git branch --show-current
```

Stop if the worktree is not clean.

---

## Output to inspect

Inspect this output directory read-only:

```text
D:\_datefac_agent\output\agent_excel_intake_audit_348n_linyang_energy_testset
```

Inspect:

```text
manifest json
run summary json
audit_report.md
review_queue.csv
clean_data.csv
evidence_index.json
```

Do not modify output files.

---

## Diagnosis requirements

Focus on the 367 unknown rows.

Group unknown rows by:

```text
sheet name
row family / visible pattern
metric label pattern
value shape
nearby section title or table title if available in output
issue codes
whether values look numeric, narrative, metadata, table-title, or extracted testset bookkeeping
```

Answer:

```text
Are the unknown rows mostly normal financial-table rows with unfamiliar schema?
Are they mostly narrative / section-anchor / table-title rows?
Are they mostly testset bookkeeping rows generated by the Excel extraction/testset format?
Are they mostly rows that should be marked out-of-scope at this stage?
Which unknown families are safe candidates for NARRATIVE_ASSERTION routing?
Which unknown families might be safe candidates for STRICT_FINANCIAL_TABLE_ROW routing?
Which unknown families require new schema support before any routing change?
Would any obvious routing change risk polluting clean_data?
```

Do not make routing changes in this task.

---

## Scope

Allowed to create:

```text
docs/agent/348N_R1_LINYANG_UNKNOWN_ROW_SHAPE_DIAGNOSIS.md
```

Do not modify source code.

Do not modify tests.

Do not modify input or output files.

Do not run MinerU, OCR, LLM, or VLM.

Do not re-extract PDFs.

---

## Required validation

Run baseline tests:

```powershell
python -m pytest tests\agent -q
```

No py_compile is required if no Python files are changed.

---

## Expected report

Create:

```text
docs/agent/348N_R1_LINYANG_UNKNOWN_ROW_SHAPE_DIAGNOSIS.md
```

Include:

```text
Task ID
Reviewed output directory
Available files inspected
Manifest metrics recap
Unknown-row distribution by sheet
Unknown-row family taxonomy
Clean-data boundary check
Review-queue burden analysis
Out-of-scope assessment
Candidate safe refinement targets
Candidate unsafe refinement targets
Risk assessment
Baseline validation
Decision
Recommended next task
```

Decision values:

```text
348N_R1_CONFIRMED_LINYANG_UNKNOWN_ROW_FAMILIES_DIAGNOSED
348N_R1_CONFIRMED_LINYANG_TESTSET_OUT_OF_SCOPE
348N_R1_BLOCKED_BY_MISSING_OUTPUT
```

---

## Completion report

Report:

1. Files created or modified.
2. Current branch.
3. Whether worktree was clean before editing.
4. Reviewed output directory.
5. Unknown-row distribution by sheet.
6. Unknown-row family taxonomy.
7. Whether this looks like normal workbook schema or testset-specific shape.
8. Safe candidate refinement targets.
9. Unsafe candidate refinement targets.
10. Clean-data boundary check.
11. Review-queue burden analysis.
12. pytest result.
13. Whether source code was untouched.
14. Whether output files were not committed.
15. Whether LLM/MinerU/OCR calls were zero.
16. git status -sb.
17. Recommended next task.
