# Project Handoffs

This directory stores handoff notes for new chats, new models, new threads, and collaborators.

Current handoff entrypoint:

```text
docs/project_handoffs/CURRENT_MODEL_HANDOFF.md
```

Current mainline:

```text
DateFac Agent
D:\_datefac_agent
pivot/348-agent-foundation
```

Recommended use:

1. Read `CURRENT_MODEL_HANDOFF.md`.
2. Read the current task file under `docs/codex_tasks/`.
3. Read the directly related result or QA report under `docs/agent/`.
