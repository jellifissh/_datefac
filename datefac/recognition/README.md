# Recognition Package

Purpose:
- Recognition-layer helpers such as table image recognition and legacy result readers.

Not responsible for:
- end-to-end benchmark packaging

Place new files here when:
- they belong to a recognition-layer operation rather than a task wrapper

Category:
- source code

MinerU-first / table-first relation:
- recognition utilities are adjacent support components, not the main benchmark orchestration layer.
