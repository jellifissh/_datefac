# Semantic Package

Purpose:
- Semantic adjudication, alias refinement, prompt/response helpers, and semantic validation utilities.

Not responsible for:
- running MinerU CLI jobs

Place new files here when:
- they belong to semantic review, adjudication, or alias-related governance

Category:
- source code

MinerU-first / table-first relation:
- semantic helpers may sit downstream of extraction, but the current MinerU-first benchmark chain should not be confused with semantic adjudication tasks.
